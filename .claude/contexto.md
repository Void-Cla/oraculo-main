# CONTEXTO DO PROJETO — ORACULO TRADING BOT
> Mantido pelo Agente Revisor (REV). Última atualização: 2026-09-18 (DA-37: painel de chaves somente leitura, autenticado e sem segredos.)
> Leia este arquivo PRIMEIRO em toda sessão para evitar retrabalho e economizar tokens.
> Não edite manualmente — apenas o Revisor escreve aqui.
> Leia também `.claude/skill.md` (lições/atalhos do projeto) na mesma carga inicial.

---

## ESTADO ATUAL DA SESSÃO

```
Fase ativa:        F1+F2+F4+F5+F9 (✅) + F7 observabilidade parcial → próxima: conectar sombra sklearn observacional e corrigir bugs financeiros já confirmados sob GRD
Último agente:     REV (DA-37: revisão do painel leigo e status seguro de chaves IA)
Último commit:     9858af6 "upp" (mudanças desta sessão + sessões anteriores ainda não commitadas)
Testes:            19 passou / 0 falhou / 19 focados verificados pelo REV (painel, sessão e API; 2026-09-18)
                   145 verdes reportados nesta etapa; suíte integral não reexecutada pelo REV
God-file:          testnet_auto_trader.py ~2470 linhas (2 clusters extraídos p/ autotrader/)
Mypy erros:        não medido (mypy não instalado — ver FASE 9)
Cobertura atual:   não medida (pytest-cov não instalado)
Python do venv:    3.14 (pyproject exige >=3.11 — OK)
```

> ✅ As 10 falhas herdadas foram TODAS eliminadas em sessão anterior. Suíte 100% verde (253/253).
> `gerar_sinal_orquestrado` agora é `async def` (propagou por 4 call sites + testes) — ver DA-24.
>
> ⚠️ **DOIS BANCOS — ATENÇÃO AO DB_PATH (diagnóstico 2026-07-01, CORRIGIDO):**
> - `dados/oraculo.sqlite` (592MB, o do `.env`, DB_PATH REAL do app): **TEM DADO** — 34.674 velas,
>   29.285 features, 71.507 predictions, 71.023 outcomes, 6 símbolos (BNBBTC/BNBETH/BNBUSDT/BTCUSDT/ETHBTC/ETHUSDT).
> - `dados/oraculo.db` (176KB): banco de TESTE (usado por `pytest`), vazio de mercado (só 32 ordens antigas).
> ⚠️ Rodar walk-forward/scripts com `DB_PATH=./dados/oraculo.db` dá `simbolos=[]` (banco errado). Use `.sqlite`.
>
> 🎯 **VEREDITO DE EDGE RE-MEDIDO COM OS FIXES BUG-08..16 (2026-07-01):** o walk-forward no banco REAL
> agora acha **EDGE LÍQUIDO em ≥1 config** (antes dos fixes: NENHUM). Config positiva: **ETHUSDT h30
> alvo 0.1% → net líquido/trade +0.000295, IC 0.086, 821 trades, win 17.5% ✅**. É a ÚNICA das ~72 configs
> com net≥0, e a margem é FININHA (+0.0003/trade) — é PISTA/QUASE-EDGE, não edge robusto (cf. a pista
> BNBUSDT h60 de sessões anteriores). Passa o critério estrito de `registrar_resultado_edge`
> (IC≥0.02 ∧ n≥30 ∧ net≥0) — então rodar `ATUALIZAR_EDGE=1` ARMARIA o gate de conta real p/ ETHUSDT.
> **NÃO rodei `ATUALIZAR_EDGE=1`** (decisão de capital do dono; não armar real numa única config fina
> antes de ≥7 dias de validação testnet). Gate de real segue fechado por desenho até o dono decidir.
> A noite de run serve para: (1) ver o dual-motor ao vivo em testnet/exploração; (2) acumular MAIS dado
> p/ ver se o edge de ETHUSDT h30 se sustenta em mais folds antes de qualquer promoção.

---

## ⚠️ DIVERGÊNCIA ESTRUTURA REAL vs ALVO (LER ANTES DE QUALQUER EDIÇÃO)

O `CLAUDE.md` descreve uma **estrutura-ALVO** (`src/dominio/`, `src/execucao/`, `src/autotrader/`, `src/sinais/probabilidade/`…) que **NÃO é a estrutura real de hoje**. Usar os caminhos do CLAUDE.md leva a editar arquivos inexistentes. Use SEMPRE os caminhos REAIS abaixo. A migração para o ALVO é a Fase 6 (decomposição) — não mova arquivos antes disso.

| Conceito | Caminho REAL (hoje) | Caminho ALVO (CLAUDE.md) |
|----------|---------------------|--------------------------|
| Contratos | `src/contratos/trading.py` | `src/contratos/` ✅ já alinhado |
| Risk engine | `src/risco/risk_engine.py` | `src/dominio/risk_engine.py` |
| Filtro EV | `src/risco/filtro_ev.py` | `src/dominio/` |
| EV calculator | `src/probabilidade/ev_calculator.py` | `src/sinais/probabilidade/ev_calculator.py` |
| Calibrador | `src/probabilidade/probability_calibrator.py` | `src/sinais/probabilidade/` |
| Trade selector | `src/probabilidade/trade_selector.py` | `src/sinais/probabilidade/` |
| Engine probabilístico | `src/probabilidade/probabilistic_engine.py` | `src/sinais/probabilidade/` |
| Estratégias | `src/estrategias/*.py` | `src/sinais/estrategias/` |
| Regime | `src/meta_strategy/regime_detector.py` | `src/sinais/regime/regime_detector.py` |
| Meta-controlador | `src/meta_strategy/meta_controller.py` | `src/sinais/` |
| Consenso / signal engine | `src/sinais/consenso.py`, `src/sinais/signal_engine.py` | `src/sinais/` ✅ |
| Gerenciador de ordens | `src/executor/gerenciador_ordens.py` | `src/execucao/gerenciador_ordens.py` |
| Executor usuário | `src/executor/executor_usuario.py` | `src/execucao/` |
| God-file autotrader | `src/servicos/testnet_auto_trader.py` | `src/autotrader/` (decompor em 4) |
| Profit guard | `src/multiativo/profit_guard.py` | `src/dominio/profit_guard.py` |
| Multiativo (resto) | `src/multiativo/*` | `src/multiativo/` ✅ |
| Circuit breaker | `src/executor/circuit_breaker.py` ✅ (criado F5; wiring no loop = F6) | `src/execucao/` |
| Idempotência | `src/executor/idempotencia.py` ✅ (criado F5, ON em toda ordem) | `src/execucao/` |
| Autotrader (decomp.) | `src/autotrader/{calculos,configurador}.py` ✅ (parcial F6) + `src/servicos/testnet_auto_trader.py` | `src/autotrader/` (completar) |
| Observabilidade qualidade | `src/observabilidade/{qualidade_sinal,correlacao}.py` ✅ (F7) | `src/observabilidade/` |
| Unit of Work | `src/persistencia/uow.py` (morto) | `src/persistencia/uow.py` — reativar F4 |
| Observabilidade | `src/observabilidade/{logger,audit,metricas}.py` | `src/observabilidade/` ✅ |
| API / endpoints | `src/api/app.py` + `src/main.py` (1176 linhas) | `src/api/main.py` |

---

## PROGRESSO POR FASE

| Fase | Status | Concluída em | Agentes usados |
|------|--------|-------------|----------------|
| 0 - Mapeamento | ✅ Concluída | 2026-06-19 | ORQ, REV |
| 1 - Código morto | ✅ Concluída (4 arquivos removidos; uow.py mantido p/ F4) | 2026-06-19 | REF, TST |
| 2 - Bugs críticos | ✅ Concluída (BUG-01..06 + bug `if not False`) | 2026-06-19 | OBS, EXE, QNT, TST |
| 3 - Contratos | 🔄 Parcial (`src/contratos/trading.py` existe) | — | — |
| 4 - Matemática | ⬜ Pendente | — | — |
| 5 - Segurança | ✅ Concluída (circuit_breaker, idempotência, fail-fast) | 2026-06-19 | EXE, GRD, TST |
| 6 - Decomposição | 🔄 Foundation + 2 clusters extraídos (`autotrader/{calculos,configurador}`; god-file 2467→2371). Falta: split de `_executar_ciclo` (833 linhas — exige caracterização supervisionada) + UoW | 2026-06-20 | REF, TST |
| 7 - Observabilidade | 🔄 Quase (IC/Brier/drawdown + correlation_id prontos e testados; falta só wirear nos logs financeiros) | 2026-06-20 | OBS, TST |
| 8 - Testes | ✅ 0 falhas / 115 verde (cobertura ainda não medida) | 2026-06-19 | TST |
| 9 - CI/CD | ✅ `.github/workflows/ci.yml` + `.pre-commit-config.yaml` + `[dev]` extras | 2026-06-19 | ORQ |

> Legenda: ⬜ Pendente · 🔄 Em andamento · ✅ Concluída · ❌ Bloqueada

---

## BUGS CONFIRMADOS — RASTREIO (caminhos REAIS)

| ID | Arquivo REAL | Descrição curta | Status | Corrigido por |
|----|--------------|-----------------|--------|---------------|
| BUG-01 | `src/servicos/fluxo_usuario_sinais.py` | `logger` sem import — `NameError` no fluxo default | ✅ Corrigido (`get_logger(__name__)` + test_pipeline reescrito) | OBS |
| BUG-02 | `src/probabilidade/ev_calculator.py` | Custo aplicado 1×; agora round-trip `(fee+slippage)*2 + spread` | ✅ Corrigido (`NUMERO_DE_PERNAS=2` + test_ev_calculator) | QNT |
| BUG-03 | `src/executor/gerenciador_ordens.py:107` | `custo_total = notional*taxa` single-leg | ✅ Corrigido (×`NUMERO_DE_PERNAS`; campo não era lido) | QNT |
| BUG-04 | `src/main.py` + `src/servicos/ajustes.py` | `salvar_ajustes_testnet` descartava `notional_usdt` enviado | ✅ Corrigido (reflete `filtrado` no `aplicado`) | EXE |
| BUG-05 | `src/multiativo/orquestrador.py:225-228,240` | `ajustes_sinal.get()` sem guard `None` | ✅ Corrigido (→ `ajustes_sinal_exec.get`) | EXE |
| BUG-06 | `src/probabilidade/probability_calibrator.py` | `math.exp()` `OverflowError` | ✅ Corrigido (sigmoid estável, clamp `_MAX_EXP_ARG`) | QNT |
| BUG-07 | `src/servicos/testnet_auto_trader.py:947` | `if not False:` (edição pela metade) ignorava flag de stop — bug NOVO | ✅ Corrigido (`AUTO_PERMITIR_STOP_COM_PREJUIZO`) | EXE |
| INC-01 | `src/sinais/consenso.py` | Limiares assimétricos confirmar ≥0.10 vs vetar ≥0.35 (viés a abrir trade) | ✅ RESOLVIDO 2026-07-01 (autorizado pelo dono do projeto, após fix do `signal_prob_scale` eliminar a saturação do modelo que tornava o veto estruturalmente inoperante): simetrizado para `ALINHAMENTO=VETO=0.25`, `MIN_ALINHADAS=2`/`MIN_CONTRARIAS=1`; ordem dos `elif` invertida (veto antes de confirmação) | QNT/GRD |
| BUG-08 | `src/sinais/consenso.py` (override multi-TF) | Bypass de EV: `consenso_forte and vantagem_prob>=0.56` liberava BUY/SELL ignorando `lucro_liquido_esperado < lucro_liquido_minimo` (gate de EV nos `elif` seguintes nunca era alcançado) | ✅ Corrigido 2026-07-01 (achado por auditoria externa `outros/super fix` antes de `outros/` ser removido — ver DA-25): override agora exige `ev_suficiente or force_allow`; + checagem defensiva final pós-cadeia `if/elif` | QNT |
| BUG-09 | `src/probabilidade/trade_selector.py` | Sem filtro de movimento mínimo: aceitava TP/SL abaixo do custo round-trip; viés de empate (`>=`) favorecia BUY | ✅ Corrigido: `razao_bruto_custo_minima=3.0` (só ativa se caller passar `take_profit`/`stop_loss` — compat preservada); empate agora `>` estrito → HOLD | QNT |
| BUG-10 | `src/sinais/signal_engine.py` | `signal_prob_scale=10` fazia o modelo ML saturar em ±1.0 (previsão real quase não movia a probabilidade calibrada); `signal_confirm_threshold=1` e limiar de 3bps em `_direcao_janela` tornavam a confirmação multi-timeframe decorativa | ✅ Corrigido: `scale=200`, `confirm_threshold=3`, limiar `_direcao_janela=0.0015` (15bps); ramo HOLD não reporta mais `max(ev_buy,ev_sell)` (agora `0.0`) | QNT |
| BUG-11 | `src/risco/edge_config.py` | `_edge_aprovavel` exigia só o "Caminho A" (net/trade histórico ≥0 AND IC AND n_trades) — como o walk-forward reporta net negativo em 100% das configs testadas, o gate nunca abria mesmo havendo IC positivo | ✅ Corrigido: adicionado "Caminho B" (OR, não substitui A): aprova se `bruto_necessario_pct >= EDGE_RAZAO_BRUTO_CUSTO_MINIMA(3.0) * custo_round_trip`, com `n_trades`/`ic` ainda obrigatórios. `from_dict` agora auto-expira `ativo` na desserialização | QNT/GRD |
| BUG-12 | `src/executor/circuit_breaker.py` | Docstring afirmava persistência automática, mas `registrar_resultado`/`resetar_halt` nunca chamavam `salvar()` — halt ativado em memória se perdia num crash/restart | ✅ Corrigido: núcleo síncrono preservado (marca `_precisa_persistir`); novos `registrar_resultado_e_persistir`/`resetar_halt_e_persistir` (async) persistem quando necessário. Módulo continua dormente por desenho (DA-16) | EXE/GRD |
| BUG-13 | `src/executor/idempotencia.py` | Fallback de `chave_intencao` ausente usava `time.time()` (segundo atual) — retry >1s depois gerava ID diferente, quebrando idempotência cross-restart | ✅ Corrigido: fallback determinístico (`"auto"` fixo) — mesma intenção (usuário+símbolo+lado+notional) sempre gera o mesmo ID, mesmo em segundos/restarts diferentes | EXE/GRD |
| BUG-14 | `src/autotrader/configurador.py:76` | `_usuario_virtual` forçava `max_trades_abertos=min(x,1)` sempre — anulava silenciosamente o valor real do autotrader (5), risco de travamento tipo posição-fantasma | ✅ Corrigido: `min(max(1,x), 5)` — respeita o caller dentro de [1,5] em vez de forçar sempre 1 (confirmado via grep que `testnet_auto_trader.py` configura 5) | EXE/GRD |
| BUG-15 | `src/risco/risk_engine.py` + `src/probabilidade/ev_calculator.py` | `binance_taxa_maker_pct` default 0.1 (=taker; real é 0.075, desconto padrão) superestimava custo maker em 33%; `EVCalculator.fee` default 0.0012 fictício (~20% acima do taker real 0.001) | ✅ Corrigido: `taxa_maker_pct=0.075`, `fee=0.001`. Nenhum call site dependia do valor antigo (todos passam `fee=` explícito) | QNT |
| BUG-16 | `src/calculos/gerador_features.py` | 4 features mal-normalizadas: `slope_ma` em unidade absoluta de preço (não comparável entre símbolos), `volume_medio` incluía a barra corrente (ratio sempre ~1.0), `pressao_compra/venda` brutas (não normalizadas pelo book), `_retorno` degradava horizonte silenciosamente com poucos klines (ex.: `r_15m` calculado sobre 4 barras) | ✅ Corrigido: `slope_ma` ÷ close, `volume_medio` via `shift(1)`, pressão ÷ `total_book`, `_retorno` retorna `0.0` se dados insuficientes (não degrada). Follow-on necessário: `src/estrategias/momentum.py` normalizava `slope_ma` DE NOVO (double-normalization pós-fix) — corrigido junto | QNT |
| INC-02 | `noticias.py` + `llm_analista.py` | `modelo_llm` hardcoded mesmo em fallback heurístico | ✅ Corrigido: reporta `heuristica_local` quando não veio do GPT (3 pontos) | OBS |
| INC-03 | `fee_optimizer.py` + `fluxo_usuario_sinais.py` | Taxa efetiva só no autotrader | ✅ Fonte única `aplicar_taxa_efetiva`; fluxo manual aplica via `perfil_taxas` no payload | QNT |
| INC-04 | `src/multiativo/profit_guard.py` | Recebe taxas/slippage e não os usava em gate | ✅ Corrigido: reconstrói custo round-trip + gate `margem_insuficiente_sobre_custo` (10% do custo) | QNT/GRD |
| INC-05 | `src/persistencia/repositorio_snapshot.py` | Read-modify-write sem lock/versão | ✅ `atualizar()` atômico (`BEGIN IMMEDIATE` + `versao`); 2 call sites migrados | PER |
| INC-06 | `regime_detector.py` vs `repositorio_features.py` | Limiar `vol_regime` divergente (0.003 vs 0.0035) | ✅ Corrigido: fonte única `src/core/constantes_mercado.py` (0.0035 canônico) | SIN/PER |
| PERF-01 | `src/modelagem/preditor.py` + `gerenciador_modelo.py` | `joblib.load` a cada ciclo | ✅ `obter_gerenciador_modelo` cacheado por símbolo (invalida por mtime; preserva online) | EXE/QNT |
| SEC-01 | `src/servicos/sessoes.py` | `SESSION_STORE_CREDENTIALS=True` guarda api_key/secret em texto puro em dict global | 🟡 Mitigado: credenciais JÁ são limpas no logout (`encerrar_sessao`) e na expiração (`_limpar_expiradas_sem_lock`); `/v1/sessao/status` passou a ler `incluir_credenciais=False` (segredo não sai do store). Invariante travado por 4 testes. O armazenamento em memória durante a sessão ativa é REQUISITO do loop autônomo (parecer: não flipar default sem supervisão) | GRD/EXE |
| EDGE | `src/risco/edge_config.py` (novo) + `testnet_auto_trader.py` (BUY real) | Não havia gate de LUCRATIVIDADE: conta real poderia abrir posição sem edge validado | ✅ Implementado: gate default-closed + frescor (auto-expira) + fail-closed; só abertura REAL passa (SELL/exit nunca); alimentado pelo walk-forward (`ATUALIZAR_EDGE=1`); API `/v1/edge` + `/v1/diagnostico`. Hoje registro VAZIO ⇒ entrada real bloqueada por desenho (honesto: sem edge nos dados). +14 testes | GRD/QNT/EXE/TST |
| INC-07 | `src/risco/filtro_ev.py:56-57` | Slippage aplicado 1× (single-leg) enquanto EVCalculator/profit_guard usam round-trip → EV inflado no gate de risco | ✅ Corrigido: `custo_slippage = valor_ordem * slippage * 2` (DA-02) + teste de invariante `test_custo_taxa_e_slippage_sao_round_trip` | QNT |
| FLX-01 | `src/servicos/testnet_auto_trader.py` (BUY ~2214 / SELL ~2257) | **Posição-fantasma na FONTE**: ordem não preenchida (status NEW/EXPIRED/REJECTED, executedQty=0) abria/encerrava ciclo com qtd teórica → `ciclo_ativo` sem ativo real → trava `limite_trades_abertos` + PnL falso. (causa-raiz #2 do run 13-18h) | ✅ Corrigido: `_ordem_foi_preenchida()` guarda as DUAS pernas antes de mutar o ciclo; reconciliação assume fill atrasado. +2 testes | EXE/GRD |
| FLX-02 | `src/servicos/testnet_auto_trader.py:_registrar_fechamento_ciclo` | `regime/estrategia` lidos de `ultimo_sinal` (que é STRING) → `AttributeError` silenciado por `except: pass` → resultado da ordem ficaria sem rótulo de ML. Bug latente (bloco antes inalcançável: `ciclo_ordem_id_*` nunca era setado) | ✅ Corrigido: captura `ciclo_regime`/`ciclo_estrategia` na entrada + log no lugar do swallow. **P2 agora seta `ciclo_ordem_id_*`** → bloco ativo | EXE/OBS |
| P2 | `src/servicos/testnet_auto_trader.py` (BUY/SELL exec) | Auto-trader executava ordens mas NÃO persistia em `ordens` → UI mostrava 0 e ML sem feedback por-ordem | ✅ Corrigido: `_persistir_ordem_executada` grava cada fill em `ordens` (EXECUTADA) + seta `ciclo_ordem_id_*`; `RepositorioOrdens.obter/listar` agora retornam colunas de resultado (lucro/regime). +3 testes | PER/EXE/TST |
| BUG-17 | `src/main.py` (`/v1/auto/start`) | Resolve de `capital_pct`→`notional_usdt` chamava `ClienteBinance(sessao)` POSICIONAL (construtor é kwargs-only) → TypeError engolido por `except Exception` mudo → fallback silencioso $10. Efeito em cadeia: perfil "mini" = 50%×$10 = compras de $5 exatos; saída travava em `notional_abaixo_do_minimo_saida` ($4.88<$5.05) e `lucro_minimo` $0.01 exigia 0.2%+ de movimento → ciclos de 2h-2.5h medidos (`ordens.duracao_ms` 2026-07-12). Diagnóstico: 599 sinais BUY em 18min no log, 1714 `sem_execucao` vs 8 execuções no audit. NÃO era gate — era sizing | ✅ Corrigido: helper `_notional_por_capital_pct` (kwargs corretos + testnet da sessão + `fechar()` em finally); fallback $10 mantido como fail-safe de capital mas agora LOGADO (`capital_pct_nao_resolvido_usando_fallback`); constante `_NOTIONAL_FALLBACK_USDT`. +1 teste com fake kwargs-only (`_ClienteBinanceKwargsOnly`) que reproduz a assinatura real — o fake antigo aceitava posicional e mascarava o bug | ORQ/GRD/TST |

---

## ARQUIVOS MORTOS — STATUS (verificar por IMPORT, não por substring)

| Arquivo REAL | Razão | Refs externas | Status |
|--------------|-------|---------------|--------|
| `src/servicos/coletor_noticias.py` | Stub `calcular_peso_sentimento` → 0.0 | 0 ✅ | ✅ REMOVIDO |
| `src/binance_api/coletor_velas_15s.py` | Scaffold, velas falsas | 0 ✅ | ✅ REMOVIDO |
| `src/binance_api/coletor_velas_ws.py` | Ignora flag `"x"` de vela fechada | 0 ✅ | ✅ REMOVIDO |
| `src/persistencia/base.py` | ABC sem herdeiros (verificado por import: 0; os 4 hits de "base" eram `estrategias/base.py`, vivo) | 0 ✅ | ✅ REMOVIDO |
| `src/persistencia/uow.py` | UoW implementado, nunca usado | 0 ✅ | ⏸️ MANTIDO — infraestrutura planejada p/ F4 (reativar, não recriar) |
| `src/backtester/simple_backtester.py` | Backtester pandas básico, substituído pelo walk_forward | 0 ✅ | ✅ REMOVIDO (limpeza estrutural 2026-06-21) |
| `src/calibracao/ewls.py` | Stub no-op `CalibradorEWLS` (placeholder, nunca implementado) | 0 ✅ | ✅ REMOVIDO (limpeza estrutural) |
| `src/modelagem/treinador_batch.py` | `treinar_batch` real (produz modelo_batch.joblib que o preditor carrega) | 0 (out-of-band) | ⏸️ MANTIDO — infra de treino batch (útil p/ pesquisa de edge) |

---

## DECISÕES ARQUITETURAIS REGISTRADAS

| ID | Decisão | Razão | Data |
|----|---------|-------|------|
| DA-01 | Limiares de vol_regime centralizados (fonte única) | Dois módulos divergem (0.003 vs 0.0035) | 2026-06-19 |
| DA-02 | Fee round-trip = 2× em todo EV e simulação | Binance cobra entrada E saída | 2026-06-19 |
| DA-03 | Circuit breaker reset só por ação humana | Halt automático reversível = risco de ruína em loop | 2026-06-19 |
| DA-04 | UoW obrigatório para operações multi-repositório | Sem transação → estado inconsistente em crash | 2026-06-19 |
| DA-05 | Client order ID determinístico via hash de intenção | Binance rejeita clientOrderId duplicado = idempotência grátis | 2026-06-19 |
| DA-06 | NÃO mover arquivos para a estrutura-ALVO antes da Fase 6 | Renomear em massa quebra imports e mistura "organizar" com "refatorar" | 2026-06-19 |
| DA-07 | Validação de código morto SEMPRE por import (`from x import`/`import x`), nunca por substring grep | `grep "base"` deu 22 falsos-positivos | 2026-06-19 |
| DA-08 | Limiares de regime centralizados em `src/core/constantes_mercado.py` (ALVO: `dominio/`, mover na F6) | Fonte única elimina INC-06; `core/` é leaf sem ciclo de import | 2026-06-19 |
| DA-09 | NÃO simetrizar limiares de consenso (INC-01) sem backtest — só nomear+documentar+calibrar na F8 | Alterar threshold de risco sem dado empírico é, por si, um risco financeiro | 2026-06-19 |
| DA-10 | profit_guard valida custo de forma independente (defense in depth) — gate de margem 10% do custo round-trip | PSF-02: nenhuma camada confia cegamente na anterior | 2026-06-19 |
| DA-11 | Módulos de segurança em `src/executor/` (não `execucao/`) — circuit_breaker.py, idempotencia.py | Mantém todo o pacote de execução real coeso; evita split-brain executor/execucao | 2026-06-19 |
| DA-12 | Idempotência ON por padrão: `criar_ordem_*` geram `newClientOrderId` se o chamador não passar | PSF-03: nenhuma ordem sai sem proteção contra double-submit | 2026-06-19 |
| DA-13 | Circuit breaker persiste estado em `RepositorioConfig` (chave `circuit_breaker_estado`); reset só humano | PSF-04/DA-03: halt sobrevive a restart; reset automático = risco de ruína | 2026-06-19 |
| DA-14 | CI: lint/format/mypy são advisory (continue-on-error); testes + validar_config são o gate bloqueante | Código legado não nasceu sob ruff/black; reformatar tudo numa sessão = diff gigante/arriscado | 2026-06-19 |
| DA-15 | `exigir_config_valida()` é chamado no `lifespan` (startup) — fail-fast real (PSF-01) | Config insegura (real sem chave, DB de teste em real, drawdown inválido) deve impedir o boot, não ser descoberta horas depois | 2026-06-21 |
| DA-16 | Halt financeiro canônico = breaker de perda diária inline + `retomada_operacoes_bloqueadas` persistido. `circuit_breaker.py` (drawdown%) NÃO é wireado — fica reservado p/ consolidação supervisionada futura | Dois breakers em paralelo = segunda-verdade/redundância; o inline já é durável (sobrevive restart) e reset-humano. Na dúvida → não duplicar caminho financeiro | 2026-06-21 |
| DA-17 | `daily_loss_usdt` reseta no virar do dia operacional (`daily_loss_data`), mas NÃO destrava `circuit_tripped` | "Perda diária" tem de ser diária (senão vira cumulativa e trava cedo em run multi-dia); já o halt de um dia ruim continua exigindo revisão humana (DA-03) | 2026-06-21 |
| DA-18 | Modo exploração (`AUTO_MODO_EXPLORACAO=true`) torna o micro-trading 1-15m operacional relaxando pisos de lucro/EV, mas é TESTNET-ONLY por construção (recusa engatar se conta real ligada; `_usuario_virtual` força `permitir_ev_negativo=False` em conta real) | Atende ao pedido de operar 1-15m sem jamais arriscar dinheiro real em EV negativo (dado prova EV<0). Dupla trava + log alto. Validado ao vivo: bot opera, mas racionalmente segura posição (não vende no prejuízo líquido) — confirma no-edge | 2026-06-21 |
| DA-19 | Gate de EDGE para conta real (`src/risco/edge_config.py`): abertura REAL exige edge líquido validado (walk-forward) E fresco. Default-CLOSED, auto-expira por frescor, fail-closed no erro. Só ENTRADA real passa; SAÍDA (SELL) nunca. Alimentado pelo runner do walk-forward (`ATUALIZAR_EDGE=1`); persiste em `RepositorioConfig` (chave `edge_config`) | "Esperto, rápido, seguro, lucrativo e principalmente seguro": o bot só arrisca capital quando há lucratividade PROVADA out-of-sample, e auto-ativa quando ela surge. Camada INDEPENDENTE (defense in depth), além de PERMITIR_CONTA_REAL. Hoje registro vazio ⇒ real bloqueado (honesto: sem edge) | 2026-06-21 |
| DA-20 | `/v1/sessao/status` lê sessão com `incluir_credenciais=False` (segredo não sai do store em caminho que não o usa). NÃO flipar o default global de `incluir_credenciais` (autotrader precisa de credenciais no loop) | SEC-01 mitigação segura: reduz superfície de vazamento sem quebrar o trading autônomo. Limpeza de credenciais no logout/expiração já existia; agora travada por testes | 2026-06-21 |
| DA-21 | Kill-switch financeiro file-based (`core/kill_switch.py`) verificado em `gerenciador_ordens.criar_ordem_*` ANTES de todo create_order. Override de qualquer gate; `touch $KILL_SWITCH_PATH` bloqueia tudo, `rm` libera (humano). Falha de FS = fail-safe (bloqueia) | Botão de pânico INDEPENDENTE: parar o bot em segundos sem restart nem deploy. Camada ortogonal (defense in depth) — não depende de DB, sessão, ou loop | 2026-06-21 |
| DA-22 | Integridade de modelo HMAC-SHA256 (`core/integridade_modelo.py`) opt-in por `MODELO_HMAC_KEY`: load verifica `.sig` antes do unpickle (CRIT-SEC-01 anti-RCE). Sem chave = comportamento legado (não quebra); com chave = fail-closed (artefato sem sig válido é recusado) | `joblib.load` é unpickle = RCE se `MODEL_DIR` adulterado. Opt-in evita quebrar ambientes sem chave, mas dá a capacidade militar quando ligada | 2026-06-21 |
| DA-34 | **Painel contábil e avaliação sklearn em sombra.** A primeira versão passou a separar patrimônio, FIFO e variação de sessão; DA-36 substitui o escopo patrimonial pelo saldo integral e endurece a regra de conciliação. `AvaliadorLocal` persiste previsões/resultados sklearn append-only, idempotentes e no horizonte; Brier/Wilson são descritivos, `peso_aplicado=0.0` e temperatura é inaplicável ao sklearn. Sem wiring em sinal, consenso, risco ou execução. Testes focados: 44 verdes; GRD aprovou escopo. | 2026-09-17 |
| DA-35 | **Contrato direcional sklearn isolado.** `contrato_direcao.py` congela snapshot de velas fechadas, livro sequenciado e dados exógenos datados; `treinador_direcao_sombra.py` separa símbolo/horizonte/schema, prevê antes do rótulo, treina apenas após o horizonte e abstém até aquecimento. `GerenciadorModelo.predict()` exclui o SGD online da saída servida. Nenhum dos novos módulos tem caller fora de testes: ainda não coleta, não persiste, não altera consenso, risco, ordem ou conta real. | 2026-09-17 |
| DA-36 | **Patrimônio integral; lucro só conciliado.** `valorizar_saldos_reais` marca todos os balances Binance não-zero (`free + locked`), inclusive ativos fora do monitoramento; qualquer cotação USDT ausente torna o patrimônio `null`, sem omissão/fallback. O helper legado foi alinhado ao mesmo contrato. A sessão fixa fotografia integral e mostra quantidades por ativo; sua variação é patrimonial, não lucro. Mesmo com fluxo externo informado, `resultado_trading_usdt=null` e `resultado_trading_conciliado=false`: aporte/saque não prova fills, comissões por ativo, conversões nem ordens manuais. O front só exibirá resultado quando existir reconciliador integral. Testes focados painel/sessão/API: 17 verdes. | 2026-09-18 |
| DA-37 | **Chaves IA: observação autenticada, sem escrita.** A aba IA consome somente `GET /v1/sessao/ia/chaves`, exibindo por provedor se há chave no ambiente do servidor. Nenhum segredo entra no DOM, na resposta, sessão ou SQLite; não há endpoint de escrita. | 2026-09-18 |
| DA-33 | **Exploração relaxa a confirmação multi-timeframe (TESTNET-ONLY).** Diagnóstico do run 2026-07-12 noite: com o sizing corrigido (BUG-17), o bot ficou 100% em HOLD (84/84 eventos `bloqueado_por_confirmacao_multi_timeframe`) porque o mercado LOW_VOL não produzia janela ≥0.15% — mesmo com exploração ativa, que relaxava EV/lucro mas NÃO a confirmação. Decisão do dono (AskUserQuestion, opção "Relaxar só em exploração"): `_aplicar_modo_exploracao` agora escreve `signal_confirm_threshold=1` e o novo knob `signal_janela_limiar_pct=0.0008` (1 janela ≥0.08% confirma); `signal_engine.gerar_sinal_orquestrado` lê o knob com clamp ≥0.0001 e `_confirmacao_multi_timeframe` ganhou parâmetro `limiar_retorno` (default 0.0015 INTOCADO). Produção/conta real seguem 3 janelas ≥0.15%; testnet-only por construção (exploração recusa engatar fora de testnet); `edge_logico` (bruto ≥ custo+margem) continua gateando a entrada no trader. +2 testes | 2026-07-12 |
| DA-23 | Camada agêntica Gemini (`src/intelligence/`) é CONSULTIVA e só pode VETAR a abertura — nunca aprova, dimensiona ou levanta gate financeiro. FAIL-OPEN (timeout/erro/cooldown/sem-chave ⇒ PROCEED). OPT-IN (`GEMINI_API_KEY` ausente = desativada, zero mudança). Via httpx (sem SDK pesado). Injetada no autotrader após o edge gate | "Cérebro sobre o músculo": a IA reduz falsos positivos sem jamais criar risco. Se a IA falhar/alucinar/demorar, o bot mecânico segue intacto. Nenhum gate financeiro depende da IA | 2026-06-21 |
| DA-25 | `outros/` (pasta com ~3800 arquivos: `outros/binabot`, `outros/binance-main`, `outros/super fix`, `outros/ai.md`) REMOVIDA do working tree após extração de valor. `outros/super fix/oraculo_superfix_relatorio.pdf` era uma auditoria externa independente (152 arquivos, 27 bugs catalogados) sobre um snapshot próximo desta base — verifiquei cada bug citado contra o código REAL antes de aplicar (alguns já estavam corrigidos em sessões anteriores, ex. `filtro_ev.py`/`probability_calibrator.py`; os genuinamente vivos viraram BUG-08..16 acima). `outros/ai.md` era transcrição de tutorial de trading MANUAL em ações (TradingView+MCP+Telegram, operador humano) — avaliado e descartado: mercado diferente (ações vs cripto), operador diferente (humano vs bot autônomo), e o único conceito reaproveitável (scanner de oportunidades por critérios) já existe e é superior em `src/multiativo/opportunity_scanner.py`. `outros/binabot`/`outros/binance-main` eram versões antigas do projeto (pré-Oraculo), sem código não já superado. Nada de `outros/` estava rastreado no git (0 arquivos no índice) — remoção não afeta histórico | Pedido explícito do dono: "muitas coisas que não vou usar" / limpeza de arquivos mortos e fora de contexto. Todo valor extraível já foi extraído (bugs aplicados, ai.md avaliado) antes da remoção | 2026-07-01 |
| DA-27 | **Camada de IA PLUGÁVEL (Gemini/GPT/Claude) via `AI_PROVIDER`.** `src/intelligence/provedor_ia.py`: Protocol `ProvedorIA` (`analisar`/`disponivel`/`health`) + base `ProvedorIABase` (todo o cost-control compartilhado: limite dia/hora, cooldown por falhas, timeout duro, `_redigir` anti-vazamento de chave, wrapper `analisar` fail-open) + factory `criar_provedor_ia()`. Clientes: `gemini_client.py` (herda da base), `gpt_client.py` (OpenAI, `response_format` json_object, envia temperature), `claude_client.py` (Anthropic, header `x-api-key`, NÃO envia temperature — modelos novos rejeitam). Todos via httpx (sem SDK pesado, padrão do projeto). Os 3 consumidores (`pre_execution_filter`, `market_analyst`, `post_trade_auditor`) tipam `ProvedorIA` (só type hint; lógica intocada — só chamam `.analisar()`). `main.py` lifespan usa a factory; `app.state.gemini_client`→`app.state.provedor_ia`; endpoint novo `/v1/ai/provedor/saude` + alias retrocompat `/v1/ai/gemini/saude`. Env vars: `AI_PROVIDER` (default gemini), `CLAUDE_API_KEY`/`ANTHROPIC_API_KEY`+`CLAUDE_MODEL` (default `claude-haiku-4-5`), `GPT_API_KEY`/`OPENAI_API_KEY`+`GPT_MODEL` (default `gpt-4o-mini`), cost-control genérico `IA_*` (legados `GEMINI_*` ainda respeitados). Assinatura de `_chamar_api(prompt,temperature)` do Gemini PRESERVADA (mocks intactos); base define `_executar_chamada(system_prompt,user_context,temperature)` novo p/ GPT/Claude montarem mensagens por papel. +19 testes. Fail-open: sem chave do provedor escolhido → factory retorna None → camada desativada (comportamento seguro) | Pedido explícito do dono: cliente escolhe a IA (Gemini/GPT/Claude) sem tocar código — só `.env`. Extensível (adicionar provedor = 1 subclasse). Smoke test ao vivo: servidor sobe, camada ativa, 429 do Gemini tratado fail-open sem crash | 2026-07-01 |
| DA-32 | **Revisão de produção (Fable 5, time completo) — veracidade do FRONT + IA enxuta/rápida + cadência testnet.** Pedido do dono: cravar maturidade p/ produção, garantir que TODA informação do front é verdadeira e que a % do slider é o que o bot realmente usa, e IA com informação filtrada p/ decisões rápidas sem estourar cota. **ACHADOS DE VERACIDADE (front, todos corrigidos):** (F1) o slider "Capital para operar" só era enviado no START (`capital_pct` → `/v1/auto/start`); mover com o bot rodando NÃO mudava nada no backend, e o "≈ X USDT" era cálculo local do navegador nunca confirmado — AGORA: mudança no slider → debounce 800ms → `PUT /v1/auto/config` (atualiza o bot VIVO via `AUTO_TRADER.atualizar_config`) e o label mostra "✓ X USDT aplicado no bot" (valor confirmado pelo backend); no poll, o slider é sincronizado DO backend (`/v1/auto/status.notional_usdt`) quando o usuário não está arrastando (flag sliderDirty). (F2) "Taxa efetiva 0.000%": o testnet da Binance devolve commissionRates=0 (verdade da fonte), mas o motor NUNCA precifica com 0 (pisos em `aplicar_taxa_efetiva`/trader) — novo campo `taker_pct_operacional` no `fee_optimizer` espelha a taxa REALMENTE usada nas contas (efetiva>0 ? efetiva : 0.1%) e o front exibe "Taxa aplicada". (F3) "Trades hoje" era na verdade CICLOS FECHADOS na janela dos últimos 10 (`_resumo_historico_ciclos`) → rótulo "Ciclos fechados"; "Win Rate" → "Win rate · ciclos". (F4) "PnL líquido" e "Trades recentes" são do SÍMBOLO em foco (BTCUSDT), não da carteira → rótulos "· BTCUSDT". (F5) painel novo da IA DE DECISÃO no front (`/v1/ai/provedor/saude`): modelo ativo, disponível/cooldown, chamadas usadas/teto — o dono vê o throttle funcionando em vez de achar que "sumiu". **IA ENXUTA (contexto compactado):** velas 20→12 em formato array `[ts,o,h,l,c,v]` c/ floats arredondados, histórico 10→5 (só resultado/lado/lucro/regime), notícias 10→5 (título ≤90 chars), features arredondadas — payload por símbolo ~3383→~1150 chars (**-66%**), menos tokens/TPM e resposta mais rápida; helpers `_velas_compactas`/`_historico_compacto`/`_noticias_compactas`/`_features_compactas` usados pelos 3 builders. Throttle 600→300s no `.env` (pior caso ~288/dia, real bem menos pelo gate de EV — cabe em 1000 RPD c/ folga e reage 2× mais rápido). **CADÊNCIA TESTNET:** `_usuario_virtual` em testnet permite até 12 trades/h e cooldown ≥3min (antes 3/h e ≥10min — bot "lerdo" por construção); CONTA REAL mantém 3/h e ≥10min. Gates de QUALIDADE (EV, lucro mínimo, consenso, veto) intocados em ambos os modos. **CHECKLIST GRD (todos ✅):** fee round-trip no EV, circuit_breaker, idempotencia, kill_switch, edge_config fail-closed, PERMITIR_CONTA_REAL em 9 arquivos (23 pontos). Smoke real: lote de 3 símbolos → votos `fonte=gemini` com rationale lendo as velas compactas, 1 chamada, latência ~2.6s. +7 testes (2 taxa operacional, 2 cadência, 3 compactação); 1 teste antigo atualizado (travava a cadência antiga). Suíte: 312→319 | Pedido do dono: produção + veracidade do front + IA filtrada/rápida. VEREDITO honesto registrado no log da sessão: pronto p/ produção TESTNET/piloto; conta real permanece fechada por DESENHO até o edge ser provado (walk-forward + ≥7d testnet) — 100% win em ~24 micro-trades não é prova estatística | 2026-07-04 |
| DA-31 | **VERDADE FINAL sobre o 429 + trades minúsculos (investigação exaustiva com workflow + medição ao vivo da API).** Dono relatou que o Gemini AINDA estrangulava logo no boot e que os trades eram de ~$5 apesar de $45k disponíveis. Investigação (workflow de 3 trilhas + testes empíricos diretos contra a API do Google): **(1) O free tier do Gemini NÃO é generoso — foi cortado 50-80% pelo Google em dez/2025.** Medido ao vivo (`scripts/testar_chave_gemini.py` + bursts): cada MODELO tem seu próprio balde de quota DIÁRIA (RPD), por-PROJETO (não por-chave — gerar chave nova NÃO ajuda, confirmado). RPD real hoje: `2.0-flash`=0 (desligado), `3.5-flash`(=alias `flash-latest`)=**20/dia** (estoura em minutos), `2.5-flash`=250, `2.5-flash-lite`=**1000 (o maior)**. Um bot 24/7 a cada 30s faz ~2880 chamadas/dia potenciais — incompatível com qualquer balde grátis. Reset da cota: meia-noite Pacific (08:00 UTC); reboot NÃO reseta nada. **(2) Bug real no cost-control:** `provedor_ia.analisar` contava só chamadas BEM-SUCEDIDAS — um 429 (exceção) NÃO incrementava o contador, então o teto interno (`IA_MAX_CALLS_DIA`) nunca segurava as tentativas; o bot batia na rede a cada ciclo até 5 falhas seguidas. **(3) `PostTradeAuditor` disparava 1 chamada IMEDIATA no boot** (era o 1º 429 que o dono via). **(4) Trades de $5 NÃO eram bug** — `risk_engine.py:183` faz `capital_risco=min(saldo*0.5%, max_loss_trade_usdt=$0.20)=$0.20`; `notional=$0.20/stop(~2%)=~$10×confiança=~$5`. O `max_loss_trade_usdt=$0.20` (default de segurança testnet em `_usuario_virtual`) travava a posição p/ perder no máx 20 centavos, ignorando os $45k. O dono corrigiu o rumo: o tamanho deve vir da % do FRONT (slider "Capital para operar"), não de um teto hardcoded no backend. **FIXES (todos autorizados via AskUserQuestion):** (A1) modelo default → `gemini-2.5-flash-lite` (.env + `gemini_client.py`), fixado em versão específica (o alias `flash-latest` faz hot-swap p/ preview de quota baixa). (A2) THROTTLE `AI_MIN_INTERVALO_SEGUNDOS=600`: `AnalistaMercadoIA.avaliar_lote` faz no máx 1 chamada de rede a cada 10min (cache serve o resto; motor mecânico segue a cada ciclo) — ~144 chamadas/dia, cabe folgado em 1000 RPD; `PostTradeAuditor.run_loop` espera 1 intervalo antes da 1ª auditoria (sem chamada no boot). (A3) `provedor_ia.analisar` conta CADA tentativa de rede (inclusive 429) — o teto interno vira limite REAL; `IA_MAX_CALLS_DIA=50→800`, `HORA=10→60` (abaixo do RPD do Google como 2ª rede). (B) `_usuario_virtual(notional_usdt=...)`: em TESTNET, os tetos de risco escalam com o notional escolhido no front (`max_loss=notional*2%`, `risk_per_trade=1.0`, `exposição=0.95`) p/ a posição refletir a % do slider; a compra BUY passa a respeitar `notional_teto` como TETO real (`testnet_auto_trader.py:2414`). CONTA REAL mantém os freios ABSOLUTOS conservadores (perda máx $0.20, 0.5%, 20%) — inalterados e testados. +8 testes (throttle, símbolo-novo-no-intervalo, throttle=0, conta-tentativas-429, auditor-sem-boot-call, sizing testnet dirigido pela %, sizing testnet sem notional conservador, conta real ignora notional). Smoke test real: modelo 2.5-flash-lite responde 200 (sem 429), throttle engata (1 chamada/janela), 503 transitório do Google tratado fail-open. Suíte: 304→312 | Dono trouxe evidência ao vivo 2x. A verdade dura entregue sem rodeio: o free tier do Gemini é insuficiente p/ bot 24/7 — a saída é throttle agressivo (feito) OU billing pago. Trades minúsculos eram segurança correta, mas o tamanho deve ser dirigido pelo front (% da carteira), não hardcoded no backend — correção arquitetural do dono | 2026-07-02 |
| DA-30.2 | **CAUSA REAL do 429 persistente pós DA-30.1: não era mais bug de código — era o MODELO.** Depois do fix DA-30.1 (1 chamada/ciclo confirmada), o dono mostrou um log NOVO onde a PRIMEIRA tentativa de rede do processo já vinha 429, mesmo com o gate funcionando. Isolei com `scripts/testar_chave_gemini.py` (novo, script standalone fora do bot, chama a API REST direto): resposta oficial do Google era `"Quota exceeded... limit: 0, model: gemini-2.0-flash"` — quota ZERO, não "excedeu 15/min". Testado com a chave ANTIGA e uma chave NOVA recém-gerada pelo dono (AI Studio) — MESMO erro nas duas, descartando teoria de "chave vazada/abusada". Causa real: `gemini-2.0-flash` (hardcoded como default no projeto desde a v1) teve a quota gratuita ZERADA pelo Google — modelo de geração anterior, superado por 2.5/3.0/3.5. `gemini-flash-latest` (alias que sempre resolve pro Flash mais recente — hoje Gemini 3.5 Flash) tem quota ativa e funciona. TROCA: `GEMINI_MODEL` no `.env` e default em `gemini_client.py` de `"gemini-2.0-flash"` → `"gemini-flash-latest"` (nunca mais fica preso a um modelo específico que o Google pode descontinuar da faixa gratuita). SEGUNDO achado no mesmo fio: o Gemini 3.5 Flash tem "thinking" LIGADO por padrão, que consumia o `maxOutputTokens=1024` inteiro RACIOCINANDO antes de escrever a resposta — o JSON saía truncado/vazio (`finishReason=MAX_TOKENS`), aparecendo como `fonte="indisponivel"` no bot (silencioso, sem log de erro, porque tecnicamente não era falha de rede nem timeout). Fix: `thinkingConfig: {"thinkingBudget": 0}` no payload de `gemini_client.py` — desliga o raciocínio (caso de uso é sempre JSON curto/estruturado, não precisa de raciocínio profundo) e economiza tokens/cota por chamada (thinking consome do mesmo orçamento de output). Testado e confirmado seguro em `thinkingConfig` enviado a modelos SEM thinking nativo (2.5-flash-lite) — parâmetro é aceito/no-op, não quebra. Smoke test final de ponta a ponta: `avaliar_lote` real (2 símbolos) e `PostTradeAuditor.executar_ciclo()` real — ambos retornam `fonte="gemini"`/análise coerente, não mais fail-safe. Suíte: 304 (sem teste novo — mudança de configuração/payload, coberta pelos testes de contrato existentes que mockam `_chamar_api`/`analisar`) | O dono trouxe evidência ao vivo (log.txt) de que o problema persistia mesmo após o fix de código — a resposta certa foi ISOLAR com um script fora do sistema (pedido explícito do dono: "mini script simples... só para testar se estar funcionando ou não") antes de teorizar mais. Duas causas empilhadas (quota-zero do modelo antigo + thinking do modelo novo) só apareceram uma de cada vez, cada uma exigindo teste isolado pra não confundir com a anterior | 2026-07-02 |
| DA-30.1 | **BUG DE PRODUÇÃO no gate DA-30 (achado no MESMO dia via log.txt ao vivo) — corrigido.** O dono reportou "o gemini continua gargalando" com evidência real: `Task-1602` do log mostrou 5 erros 429 CONSECUTIVOS em 1.4s até `ia_cooldown_ativado` disparar — apesar do gate de EV (DA-30) e do lote (DA-29) já estarem ativos. Investigação (reproduzido localmente com script standalone antes de qualquer fix): `avaliar_lote` é FAIL-SAFE por desenho (NUNCA lança exceção — timeout/429/erro viram voto neutro retornado, não exceção), então o `try/except` do orquestrador NUNCA pegava uma falha real; e — pior — a docstring original de `avaliar_lote` dizia explicitamente "NÃO cacheia o resultado de falha... deixa avaliar_direcional cair no caminho por-símbolo". Resultado real: 1 falha do lote (429) → todos os N símbolos elegíveis do passo 3 do orquestrador (ou o trader consultando o símbolo em foco) encontravam cache FRIO e tentavam de novo INDIVIDUALMENTE → 1 falha virava até 6 chamadas de rede extras, o EXATO OPOSTO do que o gate deveria fazer. Confirmado com script de reprodução: cenário de produção (6 símbolos elegíveis, todo `.analisar()` levanta 429) dava **7 chamadas de rede por ciclo** antes do fix. Dois fixes complementares, ambos autorizados pelo dono (AskUserQuestion, escolheu a opção A em ambos): (1) `orquestrador.py` — o `try/except` no lote não detecta falha por exceção (não existe); em vez disso, inspeciona o RESULTADO (`fonte != "gemini"` em todos os votos ⇒ lote falhou) e esvazia `simbolos_elegiveis`, garantindo que o passo 3 NUNCA rode chamada individual no mesmo ciclo em que o lote falhou. (2) RAIZ real do bug, em `market_analyst.py`: `avaliar_lote` agora CACHEIA o resultado de falha também (`_cachear_lote`, chamado em TODOS os `return` do método, sucesso ou falha), com `_LOTE_CACHE_TTL_FALHA_S=30.0` (mais curto que `_LOTE_CACHE_TTL_S=45.0` do sucesso — uma falha pode ser transitória, não vale prender o bot em HOLD pelo mesmo tempo de um voto real). `_voto_em_cache` agora escolhe o TTL pela `fonte` do voto cacheado (`"gemini"` = TTL cheio; qualquer outra = TTL de falha). Isso fecha a exposição em QUALQUER chamador (orquestrador E o caminho isolado do trader/foco), não só no orquestrador. Reprodução pós-fix: 3 ciclos consecutivos com falha total = exatamente 1 chamada de rede POR CICLO, nunca mais. +5 testes (2 novos no orquestrador simulando o cenário exato de produção com `VotoIA(fonte="erro")` sem exceção; 2 novos em `market_analyst` — o teste que "teria pego o bug" antes de ir pra produção, e o TTL de falha mais curto que o de sucesso). Suíte: 300→304 | Lição principal: **fail-safe "nunca lança" + "não cacheia falha" é uma combinação perigosa** — todo `try/except` downstream que espera pegar a falha por exceção está cego; a falha PRECISA ser um valor de retorno detectável E cacheado, não apenas engolida. Achado em produção ao vivo pelo dono, não em teste — os 300 testes anteriores não cobriam "lote falha SEM exceção" porque nenhum teste tinha esse cenário exato | 2026-07-02 |
| DA-30 | **Gate de MOMENTO CRÍTICO — IA só é consultada com EV mecânico acionável (correção do cooldown de 60min observado em produção, sugestão do dono: "oráculo faz o trabalho bruto e só então pergunta à IA").** Log de produção mostrou o padrão real: 5 falhas 429 consecutivas → `provedor_ia` ativa cooldown de 60min → bot cai para `heuristica_local` por horas. Causa raiz: a IA era consultada em TODO ciclo de TODO símbolo, mesmo quando o motor mecânico não tinha candidato nenhum (a maioria dos ciclos — sem edge, HOLD). `gerar_sinal_orquestrado` (`signal_engine.py`) já calcula `ev_buy`/`ev_sell` (EV líquido real, via `EVCalculator`, fee round-trip) ANTES de decidir chamar a IA — o gate usa exatamente esse valor: só chama `analista_ia.avaliar_direcional` se `max(ev_buy, ev_sell) > signal_min_ev` (o MESMO piso do gate de EV mecânico existente — não é constante nova/desalinhada). `montar_monitoramento_multiativo` (`orquestrador.py`) foi reprojetado em 3 passos: (1) roda o sinal mecânico PURO (`analista_ia=None`) para todos os símbolos monitorados — 100% CPU/local, sem rede; (2) filtra quem tem EV acionável usando o resultado do passo 1; (3) só os elegíveis entram no lote (`avaliar_lote`, DA-29) e são recalculados com IA (cache do lote evita rede duplicada). Confirmado seguro rodar o pipeline 2x no mesmo ciclo para os elegíveis: `GerenciadorModelo.predict` é PURO (só `partial_fit` muta estado, chamado em outro lugar/treino) e nenhuma persistência roda dentro de `signal_engine.py`/`preditor.py`. O gate individual em `signal_engine.py` é a defesa PRIMÁRIA (protege qualquer chamador, incluindo o caminho legado sem lote); o filtro do orquestrador é otimização de custo (evita reprocessar o passo 3 para quem não passaria de qualquer forma). +8 testes (3 em `test_signal_engine.py`: sem EV não consulta, com EV consulta, piso customizado via `ajustes_sinal`; +5 em `test_multiativo_orquestrador_gate_ia.py` novo, com mock de `gerar_sinal_orquestrado` — o pipeline ML real usa cache de modelo GLOBAL por símbolo compartilhado entre testes, controlar EV cru por klines seria frágil). Suíte: 292→300 | Pedido explícito do dono, com calibragem que ele delegou ao especialista (financeiro+engenharia): "não vai dar para a IA participar de todas etapas, deixar só nas etapas críticas". EV mecânico acionável é o critério mais direto e já auditável (mesmo dado que abre/nega o trade) — sem inventar heurística nova de "momento crítico" | 2026-07-01 |
| DA-29 | **Voto direcional da IA em LOTE — 1 chamada de rede/ciclo em vez de N (correção de 429 Too Many Requests, sugestão do dono).** Sintoma: Gemini free tier (15 RPM) estourando sob o fan-out de `montar_monitoramento_multiativo` (`orquestrador.py:213`, 6 símbolos monitorados × `gerar_sinal_orquestrado` por ciclo). `AnalistaMercadoIA.avaliar_lote(simbolos, saldo, noticias_por_simbolo)` novo: monta UM contexto JSON com todos os símbolos (`construir_contexto_analise_direcional_lote`), faz UMA chamada com `PROMPT_ANALISE_DIRECIONAL_LOTE` (pede um array `"votos"` de volta, um item por símbolo, mesma ordem, `simbolo` ecoado), e preenche um cache interno `{simbolo: (VotoIA, ts)}` com TTL 45s (cobre um ciclo de 30s com folga sem herdar voto obsoleto no próximo). `avaliar_direcional(simbolo=...)` — a MESMA API já usada por `signal_engine.gerar_sinal_orquestrado`, assinatura INTOCADA — passa a consultar esse cache PRIMEIRO; cache frio (símbolo fora do lote, TTL expirado, ou lote nunca chamado) cai no caminho por-símbolo ORIGINAL, byte a byte idêntico ao de antes (os 12 testes pré-existentes continuam verdes sem alteração). `orquestrador.py` ganha parâmetro `analista_ia` (antes sempre `None` nesse call site) e chama `avaliar_lote` uma vez ANTES do loop de 6 símbolos — cada `gerar_sinal_orquestrado` subsequente serve do cache, sem rede nova. `testnet_auto_trader.py` passa `self._analista_ia` (a MESMA instância injetada no lifespan) para `montar_monitoramento_multiativo` — o foco por-símbolo do trader (linha ~1988) também herda o cache do mesmo ciclo. Fail-safe do lote inteiro = HOLD/confiança 0.0 p/ TODOS os símbolos do lote (timeout/erro/JSON inválido/sem chave) — e esse resultado de falha NÃO é cacheado (deixa cair no caminho por-símbolo, que tem seu próprio fail-safe já testado, em vez de fixar HOLD por até 45s). +14 testes (11 em `test_intelligence_market_analyst.py`: 1 chamada cobre N símbolos, cache hit sem nova chamada, cache frio cai no individual, TTL expira e refaz, desabilitado, lote vazio, JSON inválido, formato sem `"votos"`, símbolo ausente na resposta, timeout; +4 em `test_intelligence_context_builder_lote.py` novo). Suíte: 278→292 | Sugestão do dono (agregar tudo num JSON e mandar de uma vez, em vez de picotado por símbolo) — diagnóstico correto: free tier tem RPM restrito e o fan-out multiplicava por símbolo. Reduz o pico de chamadas do ciclo de até 6 para 1, sem mudar NENHUM contrato/assinatura pré-existente (zero-risco para quem já consome `avaliar_direcional` sozinho) | 2026-07-01 |
| DA-28 | **GAP-FIN-01 (auditoria fim-a-fim): `CalibradorBandit` reescrito** — era STUB (`y_cal=y_hat*1.0`, `conf`=0.55/0.60 ALEATÓRIO por epsilon-greedy que nunca aprendia, reinstanciado por ciclo) e essa confiança falsa DIMENSIONAVA a posição (`decisor_hibrido.py:45,79`). Agora `conf` é DETERMINÍSTICA: relação sinal/ruído (`|movimento previsto|/volatilidade`) menos penalidade de spread, clampada em [0.05, 0.90]. `y_cal=y_hat` (sem distorcer previsão; calibração de escala Platt/isotônica fica p/ futuro). +6 testes de propriedade (determinismo, monotonia SNR↑/spread↓, limites). Outras correções da mesma auditoria: GAP-FLX-01 (passar TP/SL a `selector.decide` em `probabilistic_engine` + usar custo round-trip REAL do EVCalculator no filtro 3× — antes o filtro de movimento mínimo era inerte no caminho vivo), GAP-DEAD-01 (removido `_preco_compra_referencia`, 0 callers), GAP-FLX-05 (`ai_advisor` troca `except:pass` por `LOG.warning`). GAP-FLX-02/03 (arbitragem triangular e reposição BNB calculadas mas não executadas) → mantidas DISPLAY-ONLY por desenho (wirar execução = feature financeira que exige decisão do dono + testes, não bolt-on pré-run; `fee_optimizer` já condiciona desconto ao saldo BNB real, fee honesto). GAP-DEAD-02 (`calcular_brier`) → MANTIDO (é testado, infra p/ wirar em saúde de modelo quando houver outcomes com prob realizada — não é morto por teste) | Auditoria fim-a-fim a pedido do dono ("nenhuma função morre no meio do caminho"): 0 gate financeiro furado; achados eram funções calculadas-e-descartadas ou stub no caminho vivo. A confiança do sizing deixou de ser ruído | 2026-07-01 |
| DA-26 | Removida infraestrutura de deploy não-commitada: `Dockerfile`, `docker-compose.yml`, `deploy/` (Caddyfile+monitoring), `scripts/backup_sqlite.sh` — eram artefatos novos (sessão anterior, parte do blueprint `botai.md`) mas nunca commitados nem usados. Também limpos: `.codex/agents.codex` (spec de agente legado, já superado por CLAUDE.md/.claude — skill.md já apontava isso mas o arquivo sobrevivia rastreado no git), `pytest_work/*.sqlite` (5 arquivos de teste ACIDENTALMENTE rastreados no git — `.gitignore` tinha o padrão quebrado `../pytest_work/`, que não é sintaxe válida de gitignore; corrigido para `/pytest_work/`), `log.txt` (log de execução stale, já gitignored mas presente no disco) | Pedido explícito do dono: não quer Docker/deploy agora. Debris acumulado de sessões passadas que nunca deveria ter sido commitado (config de agente concorrente, artefatos de teste, logs) | 2026-07-01 |
| DA-24 | **Voto de peso igual IA×mecânico (RELAXA DA-23 parcialmente, autorizado explicitamente pelo dono do projeto).** Nova fonte `"ia_gemini"` em `consenso.py` com peso NOMINAL igual a `"estrategia"` (0.36 cada). `src/intelligence/market_analyst.py::AnalistaMercadoIA.avaliar_direcional` dá à IA um voto BUY/SELL/HOLD real (não só veto), via `VotoIA` (score_direcional/confianca/sentimento_mercado). Fail-safe por CONSTRUÇÃO: `confianca=0.0` (IA indisponível/erro/timeout/sem-chave) ⇒ `score = score_direcional*confianca = 0.0` ⇒ contribuição na média pesada colapsa a ~zero mesmo com peso nominal alto — não precisa de lógica de fallback especial. O `PreExecutionFilter` (veto puro, pós-gates) NÃO foi tocado — as duas camadas coexistem independentemente (doc no topo de `consenso.py`). `gerar_sinal_orquestrado` virou `async def` (propagou p/ 4 call sites: `orquestrador.py`, `fluxo_usuario_sinais.py`, `carga_teste_rapida.py`, `testnet_auto_trader._executar_ciclo` — todos já eram contexto async). Fonte antiga `"llm"` (heurístico de palavras-chave, NÃO é IA real) renomeada p/ `"sentimento_noticias"`, peso 0.16→0.08. `modelo`/`confirmacao`/`probabilidade` reduzidos proporcionalmente (0.22→0.09, 0.14→0.06, 0.12→0.05) p/ soma=1.00. Gate de EV (`ev_suficiente`) e checagem defensiva final INTOCADOS — a IA nunca aprova trade com EV negativo (testado explicitamente). `AnalistaMercadoIA` reusa a MESMA instância de `GeminiClient` que o `PreExecutionFilter` (injetada 1x no lifespan do `main.py`; as duas camadas dividem o mesmo orçamento de custo/hora — intencional). Novo prompt `PROMPT_ANALISE_DIRECIONAL` e `construir_contexto_analise_direcional` OMITEM o sinal mecânico do contexto enviado à IA (evita ancoragem/viés) | Pedido explícito e literal do dono do projeto: reduzir travamentos do motor mecânico ("mecânico demais") somando uma segunda opinião mais flexível da IA, com peso nominal igual — não apenas veto. Aprovado sabendo que relaxa DA-23 para esta camada nova; a proteção de veto original permanece intacta como segunda camada independente | 2026-07-01 |

---

## DESCOBERTAS PENDENTES DE INVESTIGAÇÃO

| Suspeita | Módulo | Investigar com |
|----------|--------|----------------|
| ~~`ai_advisor.py` usa `lstrip("```json")`~~ | `src/servicos/ai_advisor.py` | ✅ RESOLVIDO: `_remover_cerca_markdown` com removeprefix/suffix + 4 testes (caminho ativa só com chave OpenAI) |
| ~~`dashboard.py` instancia `ClienteBinance()` sem credenciais~~ | `src/servicos/dashboard.py` | ✅ FECHADO: seguro (retorna `{disponivel: False}`) |
| ~~`capital_manager.py` aloca 70% em capital ≤$20 com alvo 0.1%~~ | `src/multiativo/capital_manager.py` | ✅ VERIFICADO COERENTE: `_TAKE_PROFIT_ALVO` é heurística de SIZING (sem custo), não vaza p/ gate de saída; gate de custo real é profit_guard+risk_engine (round-trip). Não alterar |
| ~~`ev_calculator.py`: `fee=0.0012` round-trip?~~ | `src/probabilidade/ev_calculator.py` | ✅ RESOLVIDO (BUG-02): `(fee+slippage)*NUMERO_DE_PERNAS` round-trip |
| ~~`base.py` morto?~~ | `src/persistencia/base.py` | ✅ RESOLVIDO na F1 (removido, 0 imports) |
| Triangular arbitrage: custo por perna correto? | `src/multiativo/triangular_arbitrage.py` | ✅ VERIFICADO: aplica taxa+slippage POR PERNA no loop (3×, modelo correto p/ 3-leg) |
| **AUDITORIA FIM-A-FIM 2026-07-01 (revisão de "funções no meio do caminho" a pedido do dono):** 0 CRÍTICO, gates financeiros íntegros. Achados: | — | — |
| GAP-FIN-01: `CalibradorBandit.calibrar` é STUB (`y_cal=y_hat*1.0`, `conf`=0.55/0.60 por epsilon-greedy ALEATÓRIO que nunca aprende — reinstanciado por ciclo) | `src/calibracao/bandit.py:27-38` → `preditor.py:21-22` → `decisor_hibrido.py:45,79` | 🔧 A CORRIGIR: `p_conf` (confiança fixa/aleatória) pesa o score numérico E dimensiona a posição. Trocar por confiança heurística DETERMINÍSTICA (magnitude do movimento previsto vs vol/ruído, spread, saturação do modelo) — não fingir calibrador treinado |
| GAP-FLX-01: filtro de movimento mínimo (fix BUG-09) NUNCA dispara no caminho vivo | `probabilistic_engine.py:54` (não passa TP/SL a `selector.decide`) | 🔧 A CORRIGIR (1 linha): passar `take_profit`/`stop_loss` ao `selector.decide` em `evaluate_trade`. Atenuante: fonte "probabilidade" pesa 0.05 e gate de EV real segue intacto |
| GAP-FLX-02: arbitragem triangular CALCULADA por ciclo mas NUNCA executada nem lida por gate | `orquestrador.py:240-264` | 🟡 DECIDIR: executar (com gate de custo por perna que já existe) OU virar display-only e tirar do loop quente |
| GAP-FLX-03: reposição de BNB (fee-discount) SUGERIDA mas nunca executada | `bnb_manager.py:34-37` → `orquestrador.py:246-249` | 🟡 DECIDIR: auto-repor BNB OU rotular advisory. Atenuante: `fee_optimizer` já condiciona desconto ao saldo REAL (fee honesto, não infla EV) |
| GAP-DEAD-01: `_preco_compra_referencia` código morto (0 callers) | `testnet_auto_trader.py:353-359` | 🧹 REMOVER (absorvida por `_resumir_extrato_par`) |
| GAP-DEAD-02: `calcular_brier` código morto (0 callers) — mapa.md anuncia como métrica ativa | `observabilidade/qualidade_sinal.py:49-56` | 🧹 REMOVER ou wirar em `saude_modelo`/`/v1/diagnostico` |
| GAP-FLX-05: `except Exception: pass` engole falha de persistência de insight | `ai_advisor.py:247-248` | 🧹 trocar `pass` por `LOG.warning` |

---

## MÉTRICAS DE QUALIDADE

```
Data da medição:     2026-07-01 (após Super Fix BUG-08..16 + DA-24 voto de peso igual + limpeza)
Testes:              253 passou / 0 falhou / 253 total / 0 skipped
Cobertura global:    não medida (instalar pytest-cov)
Mypy erros (strict): não medido (instalar mypy)
Ruff violations:     não medido (instalar ruff)
Maior arquivo (LOC): src/servicos/testnet_auto_trader.py — ~2470 linhas (medir de novo — DA-24 tocou este arquivo)
2º maior:            src/main.py — ~1240 linhas (DA-24 adicionou wiring do AnalistaMercadoIA no lifespan)
3º maior:            src/servicos/noticias.py — 773 linhas (não medido de novo nesta sessão)
Total src/:          não medido de novo nesta sessão (era 13.321 em 2026-06-19; cresceu com src/intelligence/ + módulos de segurança)
IC médio:            n/d (banco de produção não versionado)
Brier score:         n/d
```

---

## PRÓXIMOS PASSOS

```
[1] PRÓXIMA AÇÃO: Projetar reconciliador append-only multiativo: fills completos, comissões convertidas,
                  depósitos/saques/conversões e inventário por ativo antes de preencher resultado de trading.
[2] BLOQUEADOR:   A consulta atual é limitada a 1000 fills de um símbolo; não prova histórico completo,
                  taxa em ativo terceiro ou fluxos externos. Não promover peso/direção/temperatura sem OOS temporal.
[3] AGENTE:       PER+QNT (contrato/reconciliação) → GRD → TST → REV; em paralelo, SIN+QNT para coleta sombra.
[4] CRITÉRIO:     resultado só não-nulo com cobertura verificável de fills, taxas e fluxos; previsão/resultados
                  sklearn imutáveis por direção/versão/horizonte e `peso_aplicado=0` até promoção explícita.
[5] PENDÊNCIAS:   F5 graceful shutdown (signal handlers — adiado, melhor com loop decomposto);
                  SEC-01 (credenciais texto puro — mitigado, não resolvido), durabilidade ajustes_testnet,
                  bugs BAIXO/MÉDIO do Super Fix não aplicados por menor severidade (gerenciador_ordens.py:
                  método IOC + fill price real; executor_usuario.py: simulação diverge da execução real;
                  detector_drift.py: std de preço em vez de retorno — ver `git log` desta sessão p/ o
                  relatório original, já removido de outros/).
[6] BANCO VETORIAL: adiar até dado real + caso de retrieval (RAG p/ Gemini). sqlite-vec/Chroma local
                  atrás de contratos/RepositorioVetorial. Não é pré-requisito hoje.
[7] MÉTRICA DA CAMADA IA (DA-24): sem dado ainda sobre se o voto de peso igual melhora taxa de
                  acerto/frequência — só foi implementado e testado estruturalmente. Recomenda-se
                  medir `/v1/ai/vetos` + novo endpoint de "votos ia_gemini no consenso" (não existe
                  ainda) após rodar em testnet por um tempo, antes de qualquer promoção p/ conta real.
```

---

## PARECERES DE EXPANSÃO E WIRING PENDENTE (decisões para revisão humana)

### Wiring do circuit breaker (DEFERIDO p/ F6 — segurança)
Módulo `src/executor/circuit_breaker.py` está pronto, persistido e testado. NÃO foi wireado no
loop porque `testnet_auto_trader.py` tem 4 pontos de submit espalhados (L~2266/2277/2332/2382) e
o registro de PnL em outro ponto — cirurgia no god-file sem supervisão poderia quebrar o próprio
mecanismo de segurança. **Plano F6 (após decompor o loop):**
- `_fase_execucao`: `if breaker.esta_em_halt(): return "HALT"` ANTES de `ger.criar_ordem_*`.
- `_fase_registro`: `breaker.registrar_resultado(pnl_liquido, capital_total); await breaker.salvar()`.
- `iniciar()`/startup: `await breaker.carregar()` (recupera halt persistido).
- Instância única no `__init__` do trader.

### MCP (Model Context Protocol) — parecer: ADIAR (não agora)
Benefício real é expor o bot a um agente externo (Claude/Gemini) como ferramentas (ex.: "status do
bot", "métricas", "halt"). É valioso para OPERAÇÃO assistida por IA — exatamente a fase futura do
usuário. Mas hoje não há consumidor; adicionar um servidor MCP agora é superfície sem uso.
**Gatilho:** quando a integração com IA começar. **Forma enxuta:** 1 servidor MCP fino expondo
funções já existentes (status/métricas/validar_config/halt) — sem nova lógica de negócio.

### n8n — parecer: ADIAR (provável overkill)
n8n é orquestração visual de workflows/integrações (alertas, webhooks, cron). O projeto já tem
FastAPI + tarefas internas + (futuro) circuit breaker. Introduzir n8n adiciona um serviço externo
para coordenar — contra "manter compacto". **Alternativa enxuta:** alertas (halt, drawdown) via um
`observabilidade/alertas.py` simples (webhook/Telegram) quando necessário, sem orquestrador externo.
**Gatilho p/ n8n:** se surgirem muitas integrações externas heterogêneas que valham UI de workflow.

### Bibliotecas — parecer: manter mínimo
Stack atual cobre o necessário. Só adicionar com benefício claro: `sqlite-vec`/`chromadb` (vetorial,
quando a Gemini entrar), `structlog` (se quiser logging estruturado mais rico que o atual). Evitar
deps transitivas como API (ex.: scipy via sklearn) — métricas de qualidade usam só numpy de propósito.

### SEC-01 (credenciais em texto puro em memória) — parecer: NÃO mexer sem supervisão
`sessoes.py` com `SESSION_STORE_CREDENTIALS=True` mantém api_key/secret em dict global. O autotrader
PRECISA das credenciais persistidas em memória para o loop em background — flipar o default p/ False
quebraria o trading autônomo. Mitigação real não é "cifrar em memória" (não protege contra dump do
processo). **Recomendações (revisar com humano):** (1) limpar credenciais no logout/expiração de
sessão; (2) reduzir TTL de sessão; (3) garantir que segredos nunca vão a log/serialização (já ok via
`core/segredos.py`); (4) avaliar manter só `secret_id` + buscar do ambiente on-demand no loop.

---

## LOG DE SESSÕES

| Data | Fase | O que foi feito | Agentes | Testes antes/depois |
|------|------|-----------------|---------|---------------------|
| 2026-09-18 | DA-37 painel IA | Revisão fechou endpoint de status de chaves que não era consumido pela aba: mostra somente presença por provedor, autenticada, sem segredo ou escrita. Carteira observacional e variação não-lucro preservadas. | REV | 19/19 focados |
| 2026-09-18 | DA-36 painel patrimonial | Patrimônio passou a exigir cotação para todos os ativos não-zero e a sessão não chama variação, FIFO ou fluxo externo de lucro; resultado de trading fica nulo até reconciliação integral. | REV | 17/17 focados |
| 2026-09-17 | DA-34 painel+sombra | Painel passou a separar patrimônio, PnL FIFO e variação de sessão; baseline por token só após valoração integral. Avaliador sklearn em sombra: append-only, horizonte, Brier/Wilson, sem peso operacional. GRD aprovou; nenhuma ordem/gate/risco alterado. | GRD, TST, REV | 44/44 focados |
| 2026-07-10 | guia-mestre | **`up.md` criado (raiz): guia mestre consolidado testnet→real** a pedido do dono ("bot lento, perdendo oportunidade; 100 USDT → 10/dia"). Zero mudança de código. Conteúdo: matemática honesta (10%/dia é insustentável; edge medido +0,03%/trade ⇒ caminho real = provar edge → compor → escalar capital ~3.300 USDT p/ 10/dia a 0,3%); diagnóstico da "lentidão" (≈70% gates de qualidade corretos, 15% cadência, 10% janela REST 30s, 5% ML inline — IA nunca bloqueia); plano de velocidade segura (WS c/ watchdog+gap-fill, ML ProcessPool, rate-limit proativo, maker orders como maior alavanca legítima de "mais trades"); LLM local Ollama como 4º provedor DA-27 (1 subclasse; bot já sobrevive sem nuvem por desenho); gaps consolidados G1–G14 priorizados (P0: 7d testnet do edge + alertas + backup + drill kill-switch, antes de QUALQUER real); anti-over-engineering; checklist de entrada 50–100 USDT c/ config sugerida; 10 regras de ouro | ORQ, GRD, QNT, SIN, EXE, PER, OBS, TST, REF, REV | 319 (sem mudança de código) |
| 2026-07-10 | up-p0-parcial | **P0 do up.md executado com autonomia total (dono ausente) — só itens ADITIVOS, sem tocar gate financeiro nenhum.** (1) `src/observabilidade/alertas.py` NOVO: Telegram + webhook genérico, fail-safe absoluto (nunca lança), cooldown por chave (anti-flood, default 300s), NO-OP silencioso sem config. Wireado em 3 pontos EXISTENTES via `disparar_alerta_background` (fire-and-forget, `asyncio.create_task`, nunca bloqueia o loop) — SEM alterar nenhuma lógica de decisão, só uma chamada aditiva logo após o estado já ter mudado: `testnet_auto_trader.py` halt de perda diária (~L1697, após `circuit_tripped=True`), halt de erros consecutivos (~L2739, idem), e `main.py::_inicializar_retomada` quando o bot SOBE já em halt persistido (~L222). +7 testes (`test_alertas.py`). (2) `src/intelligence/ollama_client.py` NOVO: 4º provedor plugável (`AI_PROVIDER=ollama`), LLM 100% local (Ollama, sem chave — sentinel não-secreto satisfaz o contrato `chave_presente` da base), endpoint `/api/chat` com `format=json`. Herda TODO o cost-control/fail-open de `ProvedorIABase` sem mudar a base (mesmo padrão de gemini/gpt/claude, DA-27). `provedor_ia.py`: `PROVEDORES_SUPORTADOS` ganha `"ollama"` + branch na factory. +11 testes (`test_intelligence_ollama.py`). Nota: o bot JÁ sobrevivia sem NENHUMA IA antes disso (DA-23) — Ollama é melhoria de disponibilidade da opinião consultiva, não requisito de sobrevivência. (3) `scripts/backup_sqlite.py` NOVO: backup online via `VACUUM INTO` (consistente sob WAL, não modifica a origem), timestamp com microssegundos (evita colisão em double-run), rotação por contagem (`BACKUP_RETENCAO_ARQUIVOS`, default 72), modo `--restaurar` (drill manual, NUNCA sobrescreve o banco em uso, roda `PRAGMA integrity_check`). Validado manualmente contra um banco SQLite descartável no scratchpad (não contra `dados/oraculo.sqlite` real — evitar I/O de 592MB desnecessário): backup ok, rotação ok (removeu o mais antigo), restore ok (`integrity_check=ok`, 1000/1000 linhas). Sem cobertura pytest (mesma convenção de `pesquisa_edge.py`/`backtest_walkforward.py`). (4) `.env.example` atualizado com `ALERTA_*`, `BACKUP_*`, `OLLAMA_*`, e `AI_PROVIDER` documentando a 4ª opção. **DEFERIDO por decisão própria, documentado (não é lacuna esquecida):** rate-limit proativo da Binance (§4.V3 do up.md) — `AsyncClient` do `python-binance` NÃO guarda a última resposta HTTP nem expõe headers (`X-MBX-USED-WEIGHT-1M`) depois de `_handle_response`; a única forma seria subclassear/monkeypatch um método interno de uma lib de terceiros que toca EXECUÇÃO de ordens — risco desproporcional ao ganho (P1, não P0) para fazer sem supervisão humana. WebSocket, ML em ProcessPool e maker orders (P1/P2) também NÃO foram tocados nesta sessão — são cirurgia maior em caminho de decisão/execução, exigem caracterização supervisionada (mesma lei já aplicada ao god-file). **NENHUM gate financeiro foi tocado**: `PERMITIR_CONTA_REAL` continua false por padrão, `edge_config` continua fechado, cadência/limiares intactos. Suíte: 319 → **337** (todas as 18 novas + 0 regressão, verificado 2x pelo orquestrador) | ORQ, OBS, GRD (revisão de escopo), SIN, TST, REV | 319 → 337 |
| 2026-06-19 | 0 | Contexto inicial criado | ORQ, REV | —/— |
| 2026-06-19 | 0 | Mapeamento real: baseline 82/92, reconciliação estrutura REAL×ALVO, correção de caminhos de bugs, criação do time completo de agentes + skill.md + ambiente | ORQ, REV | —/82-passou |
| 2026-06-19 | 1+2 | BUG-01..07 corrigidos + 4 arquivos mortos removidos + 9 testes novos (EV, calibrador). Auto-trader: restauradas calibrações testnet e teto de notional via env | OBS, EXE, QNT, REF, TST, REV | 82/92 → 101/101 |
| 2026-06-19 | 4 (parcial) | INC-06 fonte única `core/constantes_mercado.py`; INC-01 limiares nomeados+documentados (não simetrizado sem dado); INC-04 profit_guard com gate de custo independente. +4 testes | QNT, SIN, PER, GRD, TST, REV | 101/101 → 105/105 |
| 2026-06-19 | 5+9 | F5: idempotencia.py (newClientOrderId em toda ordem), circuit_breaker.py (halt persistido, reset humano), validacao_config.py (fail-fast). F9: ci.yml + pre-commit + [dev] extras. Diretriz "Tradutor/Otimizador" no CLAUDE.md. +10 testes | EXE, GRD, TST, ORQ, REV | 105/105 → 115/115 |
| 2026-06-20 | lacunas | PERF-01 (cache modelo por mtime), INC-02 (modelo_llm honesto), INC-05 (snapshot RMW atômico), INC-03 (fonte única taxa efetiva). +6 testes. Trabalho autônomo em ciclos | EXE, OBS, PER, QNT, TST, REV | 115/115 → 121/121 |
| 2026-06-20 | 7 | Observabilidade: `qualidade_sinal.py` (IC/Brier/drawdown, só numpy) + `correlacao.py` (correlation_id). +5 testes. Pareceres MCP/n8n/SEC-01 + plano de wiring do breaker registrados | OBS, TST, REV | 121/121 → 126/126 |
| 2026-06-20 | 6 (parcial) | Pacote `src/autotrader/` + extração segura de `calculos` e `configurador` (re-export, testes intactos). God-file 2467→2371. Descoberto breaker de perda diária JÁ existente (não duplicar). +2 testes | REF, TST, REV | 126/126 → 128/128 |
| 2026-06-20 | run+P1 | Analisado run 13-18h (0 trades; modelo online saturado em 18 amostras/coef 71). FIX P1: gate de amostras mínimas + guarda de divergência (`MIN_AMOSTRAS_ONLINE`). Teste de EDGE: IC=-0.03, acc 48.5% → SEM edge. Não deployei modelo batch. Segredos destrackados do git. +1 teste | EXE, QNT, OBS, TST, REV | 128/128 → 129/129 |
| — | VEREDITO | Engenharia sólida e SEGURA; LUCRATIVIDADE não é problema de código (sem edge nos dados atuais). Próximo gargalo = coleta contínua de dado real + pesquisa de edge. Ver `.claude/run_analise_2026-06-20.md` | — | — |
| 2026-06-20 | edge+saida | Harness `scripts/pesquisa_edge.py`: 24/24 configs com ret/trade NEGATIVO (fee 0,24% > sinal) → minitrading 1-15m perde por matemática. Stop-loss invertido p/ ATIVO por padrão (sair antes de prejuízo). +2 testes de saída | QNT, EXE, TST, REV | 129/129 |
| 2026-06-20 | sincronização | Auditoria de fluxo/custo. FLX-01: posição-fantasma corrigida NA FONTE (`_ordem_foi_preenchida` gate as 2 pernas antes de abrir/encerrar ciclo). INC-07: slippage round-trip em `filtro_ev` (alinhado a EVCalculator/profit_guard, DA-02). +3 testes. Verificado: fee round-trip coeso nas 3 camadas; daily-loss breaker correto (abs só em perda); convenção de unidades taxa%/slippage-decimal documentada e consistente | EXE, GRD, QNT, TST, REV | 129 → 132 |
| 2026-06-20 | sincronização-2 | Varredura multiativo+IA. ai_advisor: `lstrip("```json")`→`_remover_cerca_markdown` (removeprefix) +4 testes. Verificado COERENTE (sem alterar): capital_manager (TP alvo é só sizing, não vaza p/ gate), opportunity_scanner (taxa×2 pré-dobrada + profit_guard dobra slippage), triangular_arbitrage (custo por perna 3×), orquestrador (fluxo sinal→scanner→exec). 5 pendências de investigação FECHADAS | SIN, QNT, OBS, TST, REV | 132 → 136 |
| 2026-06-21 | botai-blindagem+gemini | **Blueprint botai.md executado (subset SEGURO + testado; 186→215).** BATCH A (blindagem AA+): kill-switch financeiro file-based (DA-21, wireado em `gerenciador_ordens`), integridade HMAC de modelo opt-in anti-RCE (DA-22, wireado no load/dump), whitelist de DDL dinâmica (CRIT-SEC-07), fix path-traversal `/v1/img` (XSS de notícias JÁ estava escapado — não-issue). BATCH B (camada agêntica Gemini, DA-23): `src/intelligence/` (client httpx fail-open+cost-control, prompts, sanitizer, guard, context_builder em repos REAIS, pre_execution_filter VETO-only, post_trade_auditor) + wiring opt-in no autotrader (pós edge-gate), lifespan e endpoints `/v1/ai/gemini/saude`, `/v1/ai/vetos`, `/v1/ai/auditorias`, `/v1/kill-switch`. BATCH D: guard COOKIE_SECURE em produção. Artefatos de deploy (Dockerfile, compose, Caddy, monitoring, backup). +29 testes. **DEFERIDOS com justificativa** (leis CLAUDE.md: sem cirurgia cega no god-file / sem complexidade especulativa): UoW (DA-04, atomicidade — Binance é fonte da verdade + reconciliação já é a rede), decomposição `_executar_ciclo`, ML→ProcessPool, slowapi/CSRF (risco de flaky + invasivo), delete de circuit_breaker.py (tested), pin de deps por hash, PostgreSQL, signal-handlers (uvicorn já faz shutdown gracioso via lifespan) | GRD, EXE, SIN, OBS, TST, REV | 186 → 215 |
| 2026-06-22 | fix-0-trades+edge-logico | **DIAGNÓSTICO run 13h (auditoria do DB):** 1 ordem em 14h. Bloqueadores reais: `nenhum_perfil_encontrou_lucro_liquido_viavel` (889×, ENTRADA) + `saldo_legado_abaixo_do_minimo_operacional` (495×). Confirmado que cooldown/rate-limit JÁ são keyed em FILL real (L2579-2585), não em tentativa — não eram o problema. **CAUSA:** `_selecionar_perfil_entrada` não relaxava piso-de-lucro/confirmação em exploração → entrada travada mesmo SEM posição aberta. **FIX:** (1) fallback de perfil em exploração quando nenhum qualifica (preserva sizing quando há perfil qualificado — sem mudar comportamento legado); (2) **EDGE LÓGICO** novo (`src/risco/edge_logico.py`): gate determinístico por-ciclo — só entra se retorno LÍQUIDO esperado cobre custo round-trip + margem e confiança ok; vira o gate de entrada em exploração, JUNTO com o veto Gemini. (3) Gemini: corrigido VAZAMENTO da API key em log (header `x-goog-api-key` + redação). +9 testes | EXE, GRD, QNT, OBS, TST, REV | 215 → 224 |
| 2026-06-21 | edge-governanca+SEC-01 | **Gate de EDGE (DA-19):** `src/risco/edge_config.py` — núcleo PURO `avaliar_edge` (default-closed, frescor/auto-expira) + `RegistroEdge` durável (RepositorioConfig) + `registrar_resultado_edge` (veredito do walk-forward → `ativo`, critério estrito: tem_edge ∧ n_trades≥30 ∧ IC≥0.02 ∧ net≥0). Wireado na abertura REAL do autotrader (`_gate_edge_conta_real`, fail-closed; SELL/testnet isentos). Runner do walk-forward grava o registro (`ATUALIZAR_EDGE=1`). API `/v1/edge` + edge em `/v1/diagnostico`. **SEC-01 (DA-20):** `/v1/sessao/status` agora `incluir_credenciais=False`; invariante de limpeza no logout/expiração travado. +18 testes. Hoje registro de edge VAZIO ⇒ entrada real bloqueada por desenho (sem edge nos dados — honesto) | GRD, QNT, EXE, OBS, TST, REV | 168 → 186 |
| 2026-06-21 | P2-persistencia | Caminho A produção: P2 COMPLETO. `_persistir_ordem_executada` grava cada execução (BUY/SELL) na tabela `ordens` + seta `ciclo_ordem_id_*` (ativa o registro de lucro/regime por ordem, FLX-02). `ciclo_ordem_id_*` init/reset em _novo_estado/_abrir_ciclo/_encerrar_ciclo. `RepositorioOrdens.obter/listar_recentes` retornam colunas de resultado. Best-effort (falha não derruba ciclo). +3 testes. Diagnóstico 5h: bot executou 2 SELLs reais mas invisíveis na UI (gap fechado agora) | PER, EXE, GRD, TST, REV | 165 → 168 |
| 2026-06-21 | log+ic-join | Verificado log.txt do restart: SEM erro real (38 linhas, todas INFO; os "401" iniciais são polls pré-login, viram 200 após /v1/sessao/entrar). Coletor contínuo confirmado rodando (3/3). Fix follow-up: o join do IC-de-retorno exigia ts exato, mas `ts_previsao` é meio-de-minuto → 0 matches; corrigido p/ floor-ao-minuto (18595 matches). IC de retorno real agora: BTC 0.089, ETH -0.034, BNB 0.003. +ajuste de 2 testes. Pendente: restart p/ o fix do IC ir ao vivo (servidor roda versão anterior; só observabilidade) | OBS, QNT, TST, REV | 165 |
| 2026-06-21 | espiada-online+fix | Espiei o servidor online (/v1/diagnostico). Confirmado: gate anti-divergência funcionando AO VIVO (online coef_norm=70.88 saturado, 18 amostras → online_em_uso=false; batch ausente → bot no fallback heurístico). **FALHA achada+corrigida:** `resumo_qualidade_recente` correlacionava PREÇO×PREÇO (IC espúrio 0.97 = falso sinal); reescrito p/ IC de RETORNO (join ohlcv ref) +2 testes. READMEs (raiz+src) reescritos fiéis ao estado atual. `.env`/`.env.example`: ATIVAR_COLETA_CONTINUA ligado | OBS, QNT, REF, REV | 164 → 165 |
| 2026-06-21 | limpeza-estrutural | Varredura completa. REMOVIDOS: `mec.txt` (⚠️ segredos em texto puro — ROTACIONAR), `log.txt` (78KB stale), `oraculo.egg-info/` (artefato build, +gitignore), `migrations/schema_mysql.sql` (schema MySQL divergente não-usado), `simple_backtester.py` (substituído por walk_forward), `calibracao/ewls.py` (stub no-op), planos defasados (`oraculo_plano_implementacao.md`, `PLANO_IMPLEMENTACAO_V3.md`), agent spec duplicado (`AGENTS.md`, `agente.codex` — canônico = CLAUDE.md + .claude/). MANTIDOS: uow.py, treinador_batch.py (infra útil). Docs ressincronizados (READMEs). Suíte intacta | REF, REV | 164 (sem mudança) |
| 2026-06-21 | edge-infra | Coletor contínuo (`tarefas/coletor_continuo.py`, env ATIVAR_COLETA_CONTINUA, reusa coletar_e_persistir, wirado no lifespan) + backtester WALK-FORWARD (`backtester/walk_forward.py` + script) com matemática de lucro LÍQUIDO correta (bruto = alvo + custo round-trip via EVCalculator). +11 testes. Veredito walk-forward: SEM edge líquido (net/trade<0 em todas configs). Pré-requisitos honestos p/ perseguir edge entregues | QNT, OBS, TST, REV | 153 → 164 |
| 2026-06-21 | api+operacional | Caminho A: micro-trading 1-15m OPERACIONAL + saídas de API. F7 wirado: `/v1/modelos/treino` (gate online, coef_norm/divergência, IC recente do banco), `/v1/ai/saude` (vida do LLM, fallback), `/v1/diagnostico` (consolidado). Módulos: `observabilidade/saude_modelo.py`, `ai_advisor.saude_llm`, status() enriquecido. Modo exploração (DA-18) testnet-only. +15 testes. Validado ao vivo: bot opera (assume/monitora posição) mas segura racionalmente no prejuízo líquido → no-edge reconfirmado. Saída inteligente (trailing+stop+lucro-mínimo) já existia e está ativa | OBS, EXE, GRD, QNT, TST, REV | 138 → 153 |
| 2026-06-21 | edge-fee | Harness ampliado (fee-sensitivity + seletividade). Melhor bruto/trade=0,056% (ETHBTC h60) < fee BNB 0,15% → FEE NÃO É O GARGALO, sinal bruto fraco demais. BTCUSDT IC≈0. Único quase-viável: BNBUSDT h60 seletivo (top 25%, acerto 79,8%, break-even após BNB) — pista, não edge (amostra minúscula, split único). Minitrading 1-15m morto por matemática. Detalhes em run_analise | QNT, REV | 138 |
| 2026-06-21 | validação-testnet | Validação ao vivo com credenciais reais (testnet, autorizado). Login OK (USDT~74k). Run de 5 ciclos: pipeline dado→sinal→decisão→auditoria→shutdown sem erro; BTCUSDT HOLD, 0 ordens (gates de custo seguraram). Round-trip real BUY+SELL $15: ambos FILLED, `_ordem_foi_preenchida`=True (FLX-01 validado contra API real), clientOrderId idempotente setado. P&L round-trip ≈0 bruto / negativo após fee = prova viva do veredito de edge. Nota: símbolos não-foco têm score_num saturado (±1.0) — não perigoso (gates usam lucro líquido), nota p/ edge | EXE, GRD, QNT, REV | 138 (sem mudança de código) |
| 2026-06-21 | run-longo | Hardening p/ runs longos. DA-15: `exigir_config_valida()` wireado no lifespan (fail-fast). DA-17: reset de `daily_loss_usdt` no virar do dia (+2 testes). DA-16: confirmado que halt financeiro persiste via `retomada_operacoes_bloqueadas` (sobrevive restart) → NÃO wirar circuit_breaker.py redundante. FLX-02: bug latente regime/estrategia (`ultimo_sinal` string) + swallow silencioso corrigidos. Verificado: SQLite WAL+busy_timeout+foreign_keys por conexão; loop com recuperação de erro + finally fecha clientes; sem crescimento de estado (históricos capados) | EXE, GRD, OBS, TST, REV | 136 → 138 |
| 2026-07-01 | voto-ia-peso-igual | **DA-24 (autorizado explicitamente pelo dono do projeto): IA passa de "só veta" p/ "voto de peso igual" no consenso.** Parte A: `src/intelligence/market_analyst.py` novo (`AnalistaMercadoIA.avaliar_direcional` + `VotoIA` dataclass fail-safe), `PROMPT_ANALISE_DIRECIONAL` + `construir_contexto_analise_direcional` (omite sinal mecânico de propósito — evita ancoragem). Parte B: `consenso.py` ganha fonte `"ia_gemini"` peso 0.36 (=`"estrategia"`); `"llm"`→`"sentimento_noticias"` peso 0.16→0.08; modelo/confirmacao/probabilidade reduzidos proporcionalmente (soma=1.00); `consolidar_decisao` ganha `score_direcional_ia`/`confianca_ia` (síncrona, pura — sem I/O); gate de EV e checagem defensiva final INTOCADOS (testado: EV negativo + IA favorável forte ⇒ HOLD). Parte C: `gerar_sinal_orquestrado` virou `async def` (propagou p/ orquestrador.py, fluxo_usuario_sinais.py, carga_teste_rapida.py, testnet_auto_trader — todos já eram async, sem cadeia nova); `AUTO_TRADER.definir_analista_ia` (padrão de `definir_filtro_ia`), reusa a MESMA instância de `GeminiClient` do `PreExecutionFilter` no lifespan de `main.py`. `PreExecutionFilter` (veto pós-gates) NÃO foi alterado — coexiste independente. +24 testes (market_analyst 11, consenso 9 — incluindo o teste mais importante: EV negativo não é bypassado pela IA —, signal_engine 3, autotrader 1). 18 lambdas de mock em `test_testnet_auto_trader.py` migradas p/ async via helper `_stub_sinal` | SIN, GRD, TST, REV | 229 → 253 |
| 2026-07-01 | multi-ia+auditoria+edge | **Sessão 2 (mesmo dia):** (1) MULTI-PROVIDER de IA (DA-27): `AI_PROVIDER=gemini\|gpt\|claude`, protocolo `ProvedorIA` + base compartilhada + 3 clientes httpx + factory; cliente escolhe a IA só pelo `.env`. +19 testes. (2) AUDITORIA FIM-A-FIM (a pedido do dono, "nenhuma função morre no meio do caminho"): 0 gate financeiro furado; achados DA-28 — GAP-FIN-01 `CalibradorBandit` era stub de confiança ALEATÓRIA que dimensionava posição → reescrito determinístico (SNR), +6 testes; GAP-FLX-01 filtro de movimento mínimo inerte no caminho vivo → TP/SL passados ao selector + custo real; GAP-DEAD-01 removido; GAP-FLX-05 log. (3) EDGE RE-MEDIDO: descoberto que rodei o 1º walk-forward no banco ERRADO (`oraculo.db` teste vazio) — o banco REAL (`oraculo.sqlite` 592MB) TEM dado (34k velas, 6 símbolos); walk-forward com os fixes de hoje acha ETHUSDT h30 marginalmente positivo (net +0.0003, IC 0.086) — PISTA fina, NÃO armei real (`ATUALIZAR_EDGE` não rodado; decisão de capital do dono). (4) `.env` ÚNICO pré-configurado (DB=.sqlite correto, Binance vazio p/ front, 3 provedores, edge/kill-switch/cost-control). (5) Smoke test ao vivo: servidor sobe, camada IA ativa, coletor 3/3, 429 do Gemini tratado fail-open sem crash. **Gemini key rate-limited (429) — provavelmente por estar vazada/em uso; rotacionar** | ORQ, SIN, GRD, QNT, TST, REV | 253 → 278 |
| 2026-07-04 | revisao-producao-fable5 | **DA-32: revisão de produção + veracidade do front + IA enxuta.** Front: slider de capital agora sincroniza AO VIVO com o backend (PUT /v1/auto/config + label "aplicado no bot" confirmado; poll sincroniza slider do backend); "Taxa aplicada" real (piso do motor, novo `taker_pct_operacional`); rótulos honestos (Ciclos fechados, Win rate·ciclos, PnL·BTCUSDT, Trades·BTCUSDT); painel da IA de decisão (modelo/cooldown/cota) visível. IA: contexto -66%/símbolo (velas 12 em array, 5 trades, 5 notícias, floats arredondados), throttle 300s. Cadência testnet: 12/h, cooldown 3min (real inalterada: 3/h, 10min). Checklist GRD 6/6 ✅. Smoke real: votos gemini com rationale, 1 chamada. VEREDITO: pronto p/ produção TESTNET/piloto; conta real fechada por desenho até edge provado | ORQ, GRD, QNT, SIN, EXE, TST, REV | 312 → 319 |
| 2026-07-02 | free-tier-real+throttle+sizing-front | **DA-31: verdade final do 429 + trades de $5** (investigação exaustiva: workflow 3 trilhas + medição ao vivo da API). Free tier Gemini medido: RPD por-modelo, por-projeto — 3.5-flash(=flash-latest)=20/dia, 2.5-flash-lite=1000 (maior). Bot 24/7 a cada 30s é incompatível com free tier. FIXES: (A1) modelo→2.5-flash-lite fixo; (A2) THROTTLE AI_MIN_INTERVALO_SEGUNDOS=600 (1 chamada/10min, mecânico segue a cada ciclo) + auditor sem chamada no boot; (A3) cost-control conta CADA tentativa (429 protege RPD), IA_MAX_CALLS_DIA 50→800; (B) tamanho de trade dirigido pela % do FRONT em testnet (`_usuario_virtual(notional_usdt=...)`, tetos de risco escalam com o notional; BUY respeita notional_teto) — conta real mantém freios absolutos. +8 testes. Smoke real: 2.5-flash-lite 200 sem 429, throttle engata | ORQ, QNT, SIN, GRD, TST, REV | 304 → 312 |
| 2026-07-02 | causa-real-modelo-quota-zero | **DA-30.2: causa REAL do 429 persistente** — dono trouxe log novo mostrando 429 na 1ª tentativa mesmo após o fix DA-30.1. Diagnosticado com script standalone (`scripts/testar_chave_gemini.py`, novo) fora do bot: `gemini-2.0-flash` tem quota gratuita ZERADA pelo Google (`limit: 0`, confirmado em 2 chaves diferentes, inclusive uma nova). Trocado default p/ `gemini-flash-latest` (alias sempre-atual). 2º achado no mesmo fio: modelo novo (Gemini 3.5 Flash) tem "thinking" ligado por padrão, estourando `maxOutputTokens` e truncando o JSON — fix: `thinkingConfig: {thinkingBudget: 0}` no payload. Smoke test real: `avaliar_lote` e `PostTradeAuditor` ambos retornam `fonte="gemini"` de verdade agora | SIN, QNT, REV | 304 (sem teste novo — config/payload) |
| 2026-07-02 | fix-critico-cache-lote | **DA-30.1: bug de produção achado AO VIVO pelo dono via log.txt** — apesar do gate DA-30, `Task-1602` mostrou 5×429 consecutivos em 1.4s. Causa: `avaliar_lote` é fail-safe (nunca lança) e NÃO cacheava falha (de propósito, na v1) → `try/except` do orquestrador nunca disparava E os símbolos elegíveis caíam no caminho individual, multiplicando 1 falha em até 6 chamadas extras. Reproduzido localmente (7 chamadas/ciclo) ANTES do fix. Fix na raiz: `avaliar_lote` cacheia falha também (TTL 30s, menor que os 45s de sucesso); orquestrador detecta falha por RESULTADO (fonte≠"gemini"), não por exceção. Pós-fix: 3 ciclos com falha total = 1 chamada/ciclo, sempre (reproduzido e confirmado). +5 testes, incluindo o que reproduz o cenário exato do log | SIN, TST, REV | 300 → 304 |
| 2026-07-01 | gate-momento-critico | **DA-30: gate de EV mecânico antes de consultar a IA** — corrige o cooldown de 60min observado no log de produção (5×429 consecutivos → `heuristica_local` por horas). Antes: IA consultada em TODO ciclo/símbolo mesmo sem oportunidade. Agora: `signal_engine.gerar_sinal_orquestrado` só chama `avaliar_direcional` se `max(ev_buy,ev_sell) > signal_min_ev` (mesmo piso do gate de EV mecânico). `orquestrador.py` reprojetado: sinal mecânico puro p/ todos → filtra elegíveis por EV → só eles entram no lote de IA (DA-29). Confirmado seguro rodar pipeline 2x/ciclo (predict é puro, sem persistência no meio). +8 testes | SIN, QNT, TST, REV | 292 → 300 |
| 2026-07-01 | voto-ia-lote | **DA-29: voto direcional da IA em LOTE** — corrige 429 Too Many Requests do Gemini free tier (sugestão do dono: agregar tudo num JSON, 1 chamada em vez de picotada por símbolo). `AnalistaMercadoIA.avaliar_lote` (1 chamada cobre todos os símbolos monitorados, cache 45s) + `avaliar_direcional` consulta o cache antes de tocar rede (fallback ao caminho por-símbolo original se cache frio — zero mudança de contrato). `orquestrador.py` (fan-out real de 6 símbolos/ciclo) e `testnet_auto_trader.py` (mesma instância `_analista_ia` do lifespan) prima o lote 1×/ciclo. +14 testes | SIN, TST, REV | 278 → 292 |
| 2026-07-01 | super-fix+limpeza | **Sessão ORQ completa: avaliação de `outros/` (4 versões antigas do projeto) + auditoria externa (PDF) + Super Fix (BUG-08..16) + redesenho da camada IA (linha acima) + limpeza estrutural.** `outros/ai.md` avaliado e descartado (tutorial de trading manual em ações, mercado/operador diferentes — DA-25). `outros/super fix/oraculo_superfix_relatorio.pdf` (auditoria linha-a-linha, 27 bugs catalogados) verificado bug-a-bug contra o código REAL (alguns já resolvidos em sessões anteriores, ~9 genuinamente vivos) e aplicado em 3 batches sequenciais de agente, cada um validado com suíte verde antes do próximo: (1) `consenso.py`+`trade_selector.py`+`signal_engine.py` — 224/224; (2) `edge_config.py`+`circuit_breaker.py`+`idempotencia.py`+`configurador.py` — 229/229; (3) `risk_engine.py`+`ev_calculator.py`+`gerador_features.py`+`momentum.py` (fix follow-on descoberto pelo agente) — 229/229 (sem testes novos, nenhum call site dependia dos defaults antigos). Depois: redesenho da IA (DA-24, ver linha acima) — 229→253. Limpeza (DA-25/26): `outros/` inteiro removido (~3800 arquivos, 0 rastreados no git), Docker/deploy não-commitado removido, `.codex/agents.codex` + `pytest_work/*.sqlite` (rastreados por engano — `.gitignore` tinha padrão quebrado `../pytest_work/`, corrigido p/ `/pytest_work/`) + `log.txt` removidos. Varredura de código morto por import: 5 candidatos, todos falsos-positivos ou infra deliberada já documentada (nada removido). Suíte final: **253/253**, verificada independentemente 2x pelo orquestrador (não só reportada pelos sub-agentes) | ORQ, QNT (×3), SIN, GRD, TST, REV | 224 → 253 (líquido desta sessão: 229→253 são os +24 de DA-24; os batches 1-2 do Super Fix não adicionaram teste novo, batch 1 ficou em 224 pois zero teste cobria os caminhos exatos dos bugs) |
